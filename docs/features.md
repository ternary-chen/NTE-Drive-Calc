# 功能原理与数据契约

本文描述当前已实现并由公共行为测试覆盖的功能。分层与数据域见 [系统架构](architecture.md)；仍需真实
环境或上游能力的部分见 [当前路线图](roadmap.md)。

## 1. 账号、设置与工作台

应用以 `AppContext` 管理当前账号和 generation。账号数据库、截图与日志互相隔离；切换账号时先停止或
阻止后台任务，再重建同步、扫描、鉴定和战报依赖。旧 generation 的 worker 结果不更新页面或数据库。

主题写入 `config/global_ui_preferences.json`，对全部账号生效；首次没有全局主题文件的新安装默认黑色主题，只有旧账号已显式保存的主题才会在首次建立时迁移。
计算偏好、基础权重、热键、扫描、倒带和 UI 选择按既定 key 写当前账号。工作台只读取账号摘要、环境状态
和当前稳定快照，不重新实现业务查询。

## 2. 库存同步与不可变快照

nte-core 库存事件经过收集、完整性检查和 `InventorySnapshotStabilizer` 内容稳定化后，一次事务提交装备、
词条、角色实例和当前指针。稳定判断使用完整内容指纹与安静窗口，不使用历史最大数量。角色列表字段缺失
与明确返回空列表属于不同快照内容；补入角色列表时，即使装备不变，也要生成新快照，不改写历史记录。

原生同步只使用已保存的原生快照作为去重基线。视觉库存切回原生同步时，即使条目内容相同，也必须保存
原生来源的新快照。同步提示按来源区分视觉库存和原生库存；视觉扫描不提供角色实例，不称为旧版 Core
快照。原生快照未附带角色列表或尚未观测到角色实例时，分别说明该快照的状态，不据此判断 Core 版本或
所有角色的极速装配可用性；装配仍按其公开校验流程解析目标角色身份。相同内容再次到达时刷新接收提示，
不重复保存快照。

稳定快照保存失败时，界面区分数据库锁冲突、只读或权限问题、容量不足、损坏、读写或打开失败、结构不匹配、
约束冲突与事务异常；无法分类时明确显示原因未知。技术详情保留 SQLite 异常类型、原始扩展错误码与错误名、
失败阶段、安全消息和回滚结果，回滚异常不覆盖首次保存错误。后台继续按既有间隔重试，不自动修库或新建账号。
日志字段和隐私边界见 [日志事件规范](reference/logging-events.md)。

`inventory.get_latest` 读取最近捕获数据，不触发强制刷新。下游启动时解析一次当前 `snapshot_id`，运行中
不追随最新指针。快照清理保护当前快照、活动配装槽位、锁定方案和未完成任务引用。

账号 schema v21 支持原生库存的运行时状态增量。残缺装备事件只覆盖当前完整快照中已知正式 UID 的
`locked`、`discarded`、`equipped`、角色实例和位置；旧 sequence 不覆盖新状态，未知 UID 被忽略，库存
成员和当前快照指针不改变。极速装配和一键弃置/锁定的已接受指令也会写入相同的当前快照投影，供仓库立即
显示目标角色/状态；后续 nte-core 事件仍覆盖同一 UID。该投影不新增、删除装备，也不推进当前快照指针。

## 3. 视觉与手柄全量扫描

鼠标和虚拟手柄是同一视觉库存能力：

```text
mouse capture  ─┐
                ├→ 有界截图流 → OCR/解析 → 完整 vision snapshot
gamepad capture ─┘
```

扫描冻结账号、generation、用户库、截图目录、静态 dataset、解析 profile、捕获驱动、窗口和 token。
OCR 初始化在首次游戏输入前完成。取消、窗口变化、截图缺失、解析失败、索引不连续或数量不一致时保留旧
当前快照，不提交半成品。

### 3.1 鼠标捕获模型

- 基准画布为 2560×1440、7 列×4 行；坐标映射到客户区顶部对齐的 16:9 物理像素内容区。
- 首屏最多 28 件；后续每页跳过重叠首行并新增 3 行、最多 21 件。
- 鼠标与虚拟手柄全量扫描在各自既有导航前，均执行一次从上向下的长按滑动，使用与全自动增量扫描相反的两点方向重置列表位置。
- 鼠标全量扫描使用用户输入总数生成不可变分页计划；末页按总行数映射到底部对齐后的四行视窗，只点击该计划格位，并用连续占用检测复核数量。虚拟手柄扫描保持原有流程。
- 首次点击前对计划首屏执行格位预检，拦截错误页签、未加载、遮挡和整体偏移。
- 普通滚动 profile 为 `-280 × 7 + -120 × 2`；每第六次翻页使用累计补偿
  `-280 × 6 + -120 × 4`。滚轮 delta 不随分辨率缩放。
- 点击位置与时序在安全范围内随机；测试可注入固定 seed。每件截图仍保存完整客户区，以兼容统一解析器。
- 双线程模式使用单截图生产者、单 OCR 消费者和 21 张有界队列；兼容低负载模式使用更慢输入和串行解析。

当前自动化覆盖 1080p、2K、4K、2560×1600 顶部对齐、末页 1–21 件及底部对齐、2000 件分页、滚动补偿、随机安全
区、背压、取消和失败不提交。真实设备行为按 [Windows 验收](validation/windows.md) 记录。

### 3.2 扫描报告与扫描后管理

每次鼠标扫描原子更新账号截图目录中的 `mouse_scan_last_report.json`，记录分辨率、profile、预检计数、
分页、滚轮、预计/捕获数量、耗时和终止状态；不记录 UID、OCR 全文、截图内容或绝对路径。

启用扫描后管理时，完整视觉快照先提交，再根据同一次冻结扫描索引生成锁定/弃置计划。执行从最大索引
倒序回退，每件依次验证原始截图身份、操作前图标、动作和操作后图标。锁定切换弃置必须等待确认弹窗。
操作前图标与固定计划不一致时，该件不执行输入、记录后继续其余计划，并在扫描完成提示中汇总；定位、
身份、弹窗或操作后复核失败仍停止后续输入。

结果写 `mouse_state_sync_last_report.json`，保存聚合数量、状态迁移、状态不一致跳过项、耗时和安全错误类型。该会话能力
不把视觉临时 UID 变为仓库可延后写回的正式 UID。

## 4. 计算、角色配置与自建角色

计算请求冻结账号、generation、快照、静态数据集、profile、角色顺序/平级组、目标配装槽位和锁快照，
返回不可变 `WeightedAllocationPreview`。保存只消费原 preview，不重新读取最新库存或配置。

候选规则：

1. 锁定真实 UID 在候选构造前联合排除；失效锁阻止计算。
2. 账号级“筛选设置”默认不选择品质和类型。选择卡带或驱动后必须同时选择至少一种品质；对已选类型，仅已选品质进入角色管理筛选，其余品质被过滤；未选类型按默认规则处理。该筛选在角色配置前执行一次，角色优先、全局最优和增量更新共用同一候选结果。
3. 驱动副词条黑名单默认是硬过滤；启用“黑名单为零权重”后不淘汰命中驱动，而是在 Top-K 评分时将黑名单词条按零权重计分。副词条自选按最深/最多命中候选池优先，组合无解时逐层放宽。
4. 卡带先执行套装与主词条硬过滤，再执行同一副词条回退；默认四件套。
5. “不限制评分等级”只移除副词条自选门槛，不移除套装、主词条，也不改变黑名单当前的硬过滤或零权重语义。
6. 平级组暴击恢复依次为只换卡带、冻结必需套装件后重选额外件、仅失败角色从零重配。
7. 角色优先中，前序组的固定 UID 不进入后续候选；最终图纸内真正等价的同分驱动则延迟归属。等价候选须保持形状、评分、词条优先级、额外形状收益及暴击约束一致；后续组可使用它们，但每次占用都保留前序全部预留槽位的一对一回填。平级组先完成联合分配，再整体建立预留；计算结束按稳定 UID 回填。普通同级矩阵搜索耗尽预留分支且没有可回填结果时，将前序预留槽位纳入同一次匹配，在原图纸候选范围内恢复完整方案，并继续核对原暴击上限等约束；已有可回填结果和显式暴击最低值路径保持原规则。虚拟占位评分为 0，不进入锁或极速装配。

计算结果只展示由固定背包快照确定的“空幕属性汇总”，不把角色当前养成拼接成预览面板。结果开始时同时
冻结各角色全部已有方案的可见配装槽位装备基线；存在装备变化时，“变动”菜单先从固定快照补全旧装备，
再按同形状、同族、同面积及稳定顺序把旧、新驱动配成同一次替换，跨形状重排也不拆成两个独立变动。旧、
新驱动与卡带都显示逐件评分和评级：优先使用方案冻结的逐件分，旧方案缺失时按当前角色评分输入统一补算。
驱动形状主词条按每格 21 攻击力、280 生命值计算，并按金/橙 1.0、紫 0.8、蓝 0.6 投影。

未显式覆盖时，官方角色使用毕业模板套装与专武；卡带主/副词条默认未选择；角色管理中的自动装备暴击率
上限计入自动选择弧盘的等级、突破及无条件 `CritBase`（含精炼常驻属性），以及已启用好感度 10 级的
`CritBase`；最小值只按装备侧暴击检查，不计好感度。角色管理中最后一次手工填写或弧盘选择覆盖当前上限，手工
填写 `0` 代表不设暴击率上限。全局最优明确忽略角色管理的
暴击率上限和最小值，切回角色优先或增量更新后恢复生效。升级保留账号
已有配置，包括主角「零」的历史“环合强度”设置。

官方角色页在“角色图纸”左侧提供账号级“家具加成”，妖刀默认提供 20 固定攻击力，拳套默认提供 4% 暴击
伤害；可按当前账号实际进度修改并随“保存”写入账号数据库。两项按正式 `AtkAdd` 和
`CritDamageBase` 进入角色面板、配装与替换、伤害公式、边际分析和战报冻结，不作为装备收益伪装计入。
角色页把养成状态直接展开：基础加成区保存好感度 10 级开关并应用正式属性修改；人物觉醒区按六个
普通效果多选，显示每项名称与说明，三/六觉共鸣按已选数量显示激活状态；技能区保存每个技能的基础等级，
详情弹窗按当前基础等级及已激活觉醒的结构化技能等级加成展示各伤害项倍率。角色页属性边际和替换收益固定
使用角色自身伤害属性及当前面板的 100% 攻击力代表直伤，不读取某个技能倍率。角色页与配装页共享的替换
弹窗按该角色直伤权重展示评分并降序排列，卡片第三项百分比表示该装备的直伤收益。基础技能为
1–10 级，三觉共鸣按正式修改提供 `+1`，生效等级最高 11 级；六觉不增加技能等级。带 `{0}` 等占位符的
觉醒说明按所引用伤害记录、关联技能和当前生效等级实时渲染。新角色默认不勾选任何觉醒；旧账号只有数值
觉醒等级时按静态顺序解析前 N 项，首次保存后固定为具体效果 ID。

人物与弧盘的等级都保留突破阶段。20/30/40/50/60/70 级显示“突破前/突破后”选择器，其他等级只有唯一
合法阶段并隐藏选择器；切换后基础面板、弧盘摘要、直伤和边际立即使用同一精确状态。账号档案显式保存
`(character_level, breakthrough_stage)` 与 `(fork_level, fork_breakthrough_stage)`，重新打开不会把临界
等级静默改成突破后。旧账号弧盘只有等级时，临界等级迁移为突破前，非临界等级迁移为达到该等级所需阶段。
schema v32 将每个可由属性/参数标识或“第一参数 + 唯一无条件 Modifier”结构证据闭合的弧盘精炼直接面板属性
持久化为静态表；施加条件、标签门槛、多候选和不完整曲线均进入构建审计而不猜值。已发布 v31 通过同一结构
解析器提供 20 件弧盘的兼容只读投影；v32 候选把相同结果持久化并重建毕业模板。两者都不依赖手工配置。
常驻属性按当前精炼与弧盘成长、突破
合并后进入角色属性汇总、直伤、毕业率和计算预览的动态边际权重。带触发、层数、持续时间或状态限制的精炼
效果继续只进入战斗状态计算。

直伤毕业率为“当前角色养成与所选配装的统一 100% 攻击直伤 ÷ 毕业基准直伤 × 100%”，结果不封顶。分母使用
满级满突破角色、满级精1默认专武、官方毕业套装和当前权重口径下的四条满值毕业副词条；两侧共同消费弧盘
无条件常驻属性、已启用的好感度 10 级正式属性、账号家具加成与额外形状。技能等级、觉醒机制、弧盘条件被动、
机制伤害和队友拐力不换算到这条统一 100% 直伤倍率；这些状态仍保存于毕业档案，供其他正式链路使用。

技能中文名由 Qt 无关的 `SkillNameRenderingService` 从静态 Ability 目录和发行版 GE 语义表解析。角色页
与后续战报均保留稳定 GA/GE 标识，只在展示层将 Ability、伤害记录和伤害元素类型映射为中文；GE 有精确
组件语义时优先展示，普通伤害项回退到所属技能中文名，没有可信名称时保留稳定标识。

账号手工基础权重是持久计算输入；未手工修改的默认权重在每次启动后按公开工坊模板更新，接口或网络异常时
使用最近有效模板或发行基线。角色详情动态权重只根据当前面板的直伤边际生成；官方角色在每次计算预览开始时
冻结这份权重用于候选排序、分析与替换，不写回基础权重。官方额外形状只读发行静态库，基础权重页仅展示且不能编辑，旧公共覆盖不参与计算；自建
角色名称、权重、额外形状、默认套装和 5×5 底盘
只写当前账号。自建底盘固定 20 个启用格位，单格可锁定。

自建角色可参与视觉库存计算和游戏界面自动装配；不进入官方角色详情、nte-core 角色实例、游戏配装导入
或极速装配。

卡带统一使用 `StatCatalog` 满级主词条值：金/橙 1.0、紫 0.8、蓝 0.6。驱动形状主词条使用相同品质系数。
方案保存
`payload.tape_main_values` 与 `payload.assignment_scores`；旧方案缺字段时才调用统一 helper 回退。

单件卡带和驱动仍按“得分 /（面积 × 10）”的比例评为 D 至 ACE。完整配装方案的总评只按总分固定区间
评定：D `<160`、C `>=160`、B `>=180`、A `>=200`、S `>=220`、SS `>=240`、SSS `>=260`、
ACE `>=280`。该总评规则不依赖角色来源或槽位：官方角色、自建角色和同角色的每个配装槽位分别按各自
方案总分评级，不改变任何单件装备的评分与评级。

## 5. 配装槽位、游戏导入与锁

每个角色最多三个可见槽位。`primary` 是兼容默认槽位，默认显示角色名；用户可新建、重命名、归档和
选择槽位。每个槽位同时只有一个当前方案，覆盖会创建新方案版本并原子更新 `current_plan_id`，历史方案
仍按自己的来源快照解析。

计算预览不显示完整角色面板。方案保存到明确槽位后，配装页才提供“角色属性汇总”：装备部分
固定读取该方案及其来源快照，角色部分读取当前等级与突破阶段、弧盘等级/突破/精炼及无条件常驻属性、
好感度 10 级、家具加成和官方额外形状。角色养成变化会更新该汇总但不改写已保存装备、逐件评分或方案总评；
带触发条件的弧盘、武器或套装效果不进入静态面板。汇总面板沿用紧邻图纸的旧版紧凑样式，不使用大字或
内嵌滚动条，前五项之外通过详情按钮查看；计算配装发生装备变动时，空幕与角色属性汇总都以旧、新、变化
三列对比前一固定方案，其中角色两侧使用同一份当前养成，只改变各自方案装备。计算页仍只有空幕属性汇总，
配装页才提供角色属性汇总。

同一角色的多个槽位是备选配装：它们可引用同一个真实 UID，不视为装备复用冲突。角色优先、全局最优和
增量更新均以角色为 UID 冲突边界；只有不同角色的当前槽位同时引用同一 UID 时处理冲突。

计算和加权计算保存前为每个角色显式选择目标槽位，可新建槽位或跳过；差异比较使用同一目标槽位，
不把其他备用槽位当成基线。批量保存是事务性的，后续角色验证失败时整批回滚。
计算结果仅提示本次第二步角色选择中“全部槽位已锁定”的角色；未选择的锁定角色不显示该提示。

游戏配装是 nte-core 稳定快照的只读投影。显式导入后保存为 `game-observed-loadout-v1`，冻结来源快照、
目标槽位、逐件评分和卡带满级值。完整驱动但缺卡带的方案可保存为 `incomplete/missing_tape`；视觉来源
不进入游戏配装导入。

锁作用于当前账号的具体槽位方案，不调用游戏锁 RPC。至少一个真实装备即可锁定；空方案和虚拟占位方案
不可锁。锁定槽位不可覆盖、归档或单件替换；其他角色/槽位不得借用其 UID。归档 `primary` 时由 DAO
提升剩余可见槽位；每个角色至少保留一个可见槽位。

配装页、角色空幕详情、替换、倒带和装配都读取选中/默认槽位。批量装配对每个角色显式选择一个 `slot_id`，
并在执行前检查角色唯一、槽位存在、原生来源和跨角色 UID 无冲突。

## 6. 仓库与鉴定

仓库读取固定快照并通过 `WarehouseInventoryService` 生成投影。卡带按套装、主属性、副属性筛选；驱动按
形状和副属性筛选。条件使用正式 ID，同组为“或”、跨组为“且”；状态分为已装备、已锁定、已弃置和其他。
重置清除全部条件，视觉来源因无可靠原生状态归入其他。

原生仓库状态管理固定快照和正式 UID，Service 生成计划，Integration 提交 RPC，随后等待递增完整快照
确认。一键弃置/锁定和仓库状态保存会在命令接受后立即更新目标 UID 的仓库投影；若这两个动作守卫期内
收到单个、覆盖全部目标 UID 的背包快照，则由动作本身替换库存集合，允许新增或移除库存。只有该动作收到
声明数量减少的背包事件时提示玩家重新在游戏登录页面同步；后台监听不会单独执行这种库存替换。视觉仓库本身只读；只有扫描完成后的同会话
鼠标管理使用扫描索引执行即时写回。

评估角色范围只影响仓库锁定/弃置评分，与计算角色、活动配装和倒带偏好隔离。

鉴定把截图、剪贴板、手工输入和仓库单件入口投影为统一装备对象，调用公共评分与
`EquipmentPresentation`，不写库存、基础权重或配装。扫描与鉴定共享 `GlobalHotkeyManager`，通过 owner
隔离会话和停止权限。

## 7. 倒带推荐与执行

工具页读取固定库存、账号偏好和活动配装槽位。打开页面或修改选项不自动求解；显式生成后返回八槽形状
多重集合。偏好字段为 `target_character_ids`、`main_character_ids`、`strategy`、`target_grade`、
`target_threshold_mode` 与 `target_custom_percent`。标准等级保留 D 至 ACE；选择“自选”后必须填写
1.0% 至 100.0% 的目标百分比，按 0.1% 精度保存到当前账号。角色选择卡统一等高；角色拥有当前计算方案时
显示其全部可见配装槽位中的最高总评分，并按完整配装 D–ACE 评级使用同色强调；游戏导入方案不作为这里的
计算分数，无计算方案时保留空白分数行。

```text
标准目标分 = 等级比例 × max(1, 驱动面积) × 10
自选目标分 = 自选百分比 × max(1, 驱动面积) × 10
缺口 = max(0, 目标分 - 方案保存的逐件评分)
```

等级比例：D=0、C=.2、B=.3、A=.4、S=.5、SS=.6、SSS=.7、ACE=.8。自选百分比按每件驱动自身的
实际面积分别计算，等于目标不形成缺口；高于目标的分数不抵消其他驱动缺口。该阈值只改变倒带推荐的
缺口统计，不改变单件评分或配装方案总评；自建角色和同角色的每个活动配装槽位均独立参与统计。

- 全面均衡：全部培养角色每种正缺分形状保留一槽，余位按“形状缺口 ÷ 库存”比例分配。
- 少角冲分：只看冲分角色，每种正缺分形状保留一槽，余位只按评分缺口比例分配。

超过八种正缺分形状时返回提示。每槽概率 12.5%；同形状重复 `q` 次时，每槽价格
`10 + 5 × (q - 1)`，总价为 `q × 每槽价格`。难度只决定蓝/紫/金品质，不进入形状、概率和价格。

显式执行冻结品质、定制模式和八槽方案。每个品质先切换对应难度再读取该难度余额；余额不跨难度复用。
初级随机十连固定 600；紫/金定制从右侧硬币口下方识别十连价格。“不做更改”保留现有候选，“应用方案”
通过轮廓与 12 种拓扑全局匹配后配置八槽。十连循环为点击、等待 1 秒、Esc、等待 1 秒、Esc、等待 0.5 秒；
执行期间可按设置中的全局停止键停止。真实执行仍为实验能力。

## 8. 极速装配与游戏界面自动装配

极速装配只消费 nte-core 正式 UID、角色实例和已保存槽位方案；游戏界面自动装配消费视觉投影与输入动作，
可支持自建角色。两条链路冻结账号、generation、来源快照、槽位列表和任务 token，并持久化角色项与事件。

极速装配每个角色最多三次完整请求。每次下发后先等待 10 秒完整递增快照；无论后续收到完整快照、范围化
装备事件或没有事件，已接受指令中的卡带/驱动都会立即投影为目标角色和格位。完整快照是首选确认来源，
可核对角色、实例和装备全集；不以驱动格位差异触发复装。范围事件仅用于发现遗漏：目标装备出现在事件中但
未装备时，才重试对应角色；没有快照或范围事件时本轮直接结束，不将其当作遗漏，也不改写库存集合。

第一次或第二次明确不一致时卸空重装，第三次仍不一致才生成最终错误。报告区分管道缺失、短暂不可用、
队列繁忙、核心请求超时、完整快照等待超时、范围事件未确认，以及角色/实例/位置/装备集合不一致。

游戏界面自动装配使用角色识别、普通鼠标输入和画面状态；角色列表、卸载、筛选、拖拽及返回均使用同一
鼠标动作 contract，并在安全检查点响应全局停止键。

## 9. 战报

战报计算页面通过独立 `nte-analysis-core.exe` 的数据库直读入口加载。请求只传冻结的账号、generation、
战报编号、数据库与配置路径、分析范围及用户候选；Rust 在只读事务中取得原始摘要、逐击、冻结养成装备、
修改副本和目标条件，执行动作与来源解析、目标识别及拟合、角色和弧盘状态、治疗、Buff、逐击公式重放、
伤害构成、反事实比较、候选收益与动态面板计算。概览、逐击、Buff 和边际页共用该入口，导出的分析投影
同样消费正式概览结果。原生计算保持已有机制覆盖边界，不把缺失证据补成零或完整收益。

同一次页面请求共享冻结事实与正式静态目录，原始基线、当前生效副本和用户候选分别计算；源角色养成、
基础属性和套装形状相同的静态规则可复用，逐击状态与候选面板不会相互污染。候选展示轴由计算结果物化，
界面不再重做整场投影。人物动态面板与驱动边际消费冻结的属性单位、角色和范围，缺失或角色不匹配时保留未知。

切换逐击详情或主页面展示角色时，相同的在途计算继续运行，完成后采用最后一次展示选择。重复选择当前
分析范围、边际编辑器初始化和退出页面不会触发额外计算。已完成结果在当前账号会话内最多保留两份，
切换战报清空；回到近期相同配置时，先校验原生计算输入再复用结果。范围、边际角色、候选配置、目标条件、
原始证据或静态规则变化均重新计算，缓存不写入账号库。

编辑器读取的角色推断事实与半场归属由 Rust 窄入口从原始逐击生成；Python 继续准备用户可编辑的养成资料、
库存和装备选择。逐击 Buff 与公式详情由 Rust 返回共享的属性、投影和采用/排除决策表，界面按事件引用组合
已有对象和说明文案，不在点击时重新计算规则；原始归属、公式来源及候选展示口径分别保留。

Python 负责用户输入、进程生命周期、取消及账号校验、结果解码和 Qt 展示。自动目标推断的
完整版本化快照由 Rust 返回，Python 在复核账号和静态配置后经既有 DAO 尽力保存；它与原始采集事实及
用户确认条件分离，供历史环境展示和后续版本匹配的恢复使用。原始摘要、逐击和冻结装备不会因分析而改写。
源码能力与实际部署状态分别核验；组件协议、失败行为及 Windows 验证边界见
[外部集成](integrations.md#11-nte-analysis-core)。

战报使用 nte-core combat 会话的聚合摘要、`battle.get_record` 和 `battle.get_axis`。背包同步与战报共享
捕获进程生命周期，不同时争抢 capture 会话。新采集要求 battle contract v5。采集期间以 500 行分页持续
暂存逐击；停止后冻结最终 record generation，从空 cursor 完整重读同一 generation 的轴，在一个事务内替换
暂存行并复核 record。停止返回后记录按契约已经冻结；代次变化、cursor 过期或 Core 报告 `complete=false`
时直接保留不完整状态，不混合 generation、不重复读取同一冻结状态，也不用摘要补造逐击。旧 v4 及更早本机战报
只保证继续查看，不回填 v5 字段。
新会话停止时若持续轮询从未取得 record，且停止响应前后也没有收到任何有效伤害摘要，则按空战报直接丢弃
暂存 capture，不再调用最终 record/axis 查询；这覆盖重录后立即“结束保存”的无战斗窗口，避免等待一个并不
存在的最终战斗记录。只要已经观测到 record 或有效伤害摘要，仍必须走完整最终轴冻结流程。
`capture.stop` 另有 12 秒硬边界；超过边界即中止本次 Core 进程、丢弃尚未完成的暂存 capture 并发布错误态，
不得让页面永久停在“正在结束”。
采集工作台使用同一位置的“开始采集”/“结束保存”按钮；只有正式采集中才显示“放弃重录”。重录会停止
当前 Core capture、删除本次暂存逐击且不生成历史记录，然后在同一账号代次下创建新的 operation 并立即开始
采集；期间保持原有背包同步暂停责任，直到新战报结束或失败后才恢复。设置页提供账号级“战报重录”全局
快捷键，默认 F11；采集会话内必须在 1.5 秒内连续按两次才执行，第一次只进入确认窗口，避免单次误触。
账号启用“保存原始抓包”后，战报在启动时冻结该开关和账号日志目录，并让 nte-core 同步保存本次 combat
会话的 `.pcapng`；关闭时不保存。原始包只用于私下排错，不写入战报数据库；采集前后复用账号级保留策略，
最多保留最近 5 份并将历史总量优先压至 512 MiB，最新一份始终保留。

账号 schema v39 保存规范化逐击、时停区间及上游 offset、可空 `pause_type_mask`、record/generation/sequence、完整性、正式追击标签、
目标上下文和原始脱敏轴行。公式输入属性同时冻结人物成长、弧盘、好感度、家具加成、装备和最终合计来源；旧记录缺少
来源分层时，历史服务只用本场冻结配置与装备重建，并显式标为当前静态库重建证据。旧战报继续保持
`summary_only`；有逐击的记录投影为 `hit_axis`。战报主页面按纵向顺序只保留采集工作台、队伍 DPS/总伤害/
战斗时长/承受伤害四项总览、统一战斗时间轴、角色贡献、角色伤害构成和审计入口；时间轴下方不再重复展示
有效伤害、有效 DPS、正式逐击、生命上限结算、时段长度和逐击事件六项指标。
采集工作台右上角的“导出战报”打开账号内历史选择页，展示采集时间、玩法、上下半场范围、逐击完整性和
保留状态，支持多选、全选与取消选择。页面显示并可保存当前账号昵称，复用“管理账号”的 `accounts[].name`
与 `database_profile.account_name`，不维护导出专用昵称；每场包内记录导出时昵称而不改写历史战报原始事实。
导出生成单个应用专用 `.ntebr` 压缩包，内部 contract 固定为 v2，包含多场完整原始摘要/record/axis、冻结养成与装备、副本指针、目标
环境和可审计派生元数据。读取同一页面选择战报包，经完整性/版本校验后将全部记录作为一个事务导入当前账号；
v2 读取器显式兼容账号库 schema 36、37、38 和 39，旧包缺失的弧盘突破阶段或时停类型保持 `NULL` 并沿用旧战报派生语义；
本地 ID 重映射、来源库存指针清空、活动配装指针不进入战报。幂等、容量和原子写入规则见
[战报包格式](reference/battle-report-package.md)。
战斗时长主值使用扣除停表后的有效时长，括号显示包含停表的真实经过时长。技能明细、分页逐击日志、Buff
和目标/血量证据进入独立审计弹窗，不在主页面铺开表格；解释性文字收进邻近的问号、提示和“详情”。逐击
日志可见列固定为时间、序号、角色、伤害项/来源技能、类型、目标、伤害、公式重放/误差、暴击判定和详情；
暴击判定只显示判定结果，不显示置信度，推算 Buff、HP 前后、公式因子、置信度及原始 ID 全部进入该击详情。
详情按已应用、未采用和待确认/结构化分组，展示命中时区间、层数、作用对象、中文属性加成、原始属性 ID、
来源定义、资产路径、置信度和采用/排除原因。

nte-core 的逐击主伤害同时保留原始 `damage` 与权威 `overkill_damage`。分析有效主伤害固定为两者之差，
追击不扣该值；顶部总伤害和 DPS 只在逐击轴完整时使用“Core 聚合原始总伤害 - 全轴权威超杀 + 全轴已计入
有效伤害的生命上限结算”，原始总伤害、超杀扣减、生命上限结算和有效总伤害在审计投影中分别保留。轴不
完整时顶部保留聚合原值，不拿残页超杀或生命结算做部分修正；旧
contract 没有该字段时也保持旧值不推断。
Core 补出的 `Server settlement residual` 默认计入当前范围有效总伤害和未归因构成，但不归给角色，也不参与
角色养成快照。完整逐击轴中若两个正式 target wire ID 具有相同最大生命，别名目标的全部已归因逐击又能按
顺序、金额和主目标 HP 缺口形成唯一闭环，Calc 只在派生分析中把两者合并为同一生命池，删除重复结算残差并
单独记录重叠扣减；原始脱敏轴行、Core 超杀和角色归属均不改写。轴不完整、候选不唯一或数值不能精确闭合时
继续保留未归因残差，不按相邻角色猜测。

统一时间轴顶部第一行集中开始/结束轴秒、确定、重置、时间口径和缩放，第二行集中角色编辑、明细范围、
当前环境/目标和环境编辑。轨外之境的“跟随 / 上半 / 下半”选择会解析为该半场的完整范围并重载上述全部
模块；新战报、历史恢复和“重置”沿用同一规则，非上下半场战报的当前范围才等于整场。所有模块共同消费
同一个左闭右开区间；页面不再建立独立“战报范围”卡片。长页概览在后台 worker 中只重建正式逐击、动作、
时间轴、角色贡献和伤害构成；逐击公式在点击逐击点时加载，Buff 区间与逐项移除反事实在打开 Buff 审计时
加载，伤害构成遇到团队倾陷时由“计算精准倾陷归属”按需加载逐角色公式，当前生效基线与候选配置的双侧公式
重放优先复用当前分析已有结果；缺少 Buff 反事实完成版本时在进入边际页后自动后台补算，候选修改后仍由
“重算”显式刷新。技能、分页日志和目标表只在对应审计
弹窗首次打开时渲染。连续切换战报或范围时只保留最新请求，账号 generation 或请求已过期的结果不投影到
页面。逐击公式、Buff 审计和边际计算等待期间，在战报页面最外层、滚动内容和主/边际页面栈之外固定显示
全宽进度底栏。整场显示 1–100% 的工作量估算进度，按请求实际包含的阶段分配占比，由核心阶段与完成事件推进，
不随时间自动增长、不因阶段切换归零，全部计算及结果整理成功后才到 100%。阶段权重按真实战报校准，
百分比不代表精确剩余时间；Buff 移除和空幕候选同时保留实际完成数。页面滚动或切到边际页时位置不变，完成、失败、请求失效、
切换范围或开始新采集时自动隐藏。用户修改副本的原始基线不重复计算仅供 Buff 审计展示的
逐项移除反事实。
统一轴只布局当前范围内的输入、动作、伤害条、逐击点和生命结算，切换半场时按该半场时长重新计算画布宽度并
回到范围左端；刻度继续使用整场绝对轴秒，保证时间仍可回查原始战报。输入/动作数量和覆盖率也只统计所选
范围。统一轴先展示低置信鼠标/键盘输入投影；随后每个角色固定一行，
上半行是该角色直伤技能条与正式逐击点，下半行是单行动作条。动作展示不因视觉重叠增行，后一个动作开始时
截断前一个动作的最小展示宽度；闪避反击和格挡反击也进入普通攻击动作行。角色行之后依次展示噩梦、蚀心、
鸩火等特殊伤害公共行，以及浊燃、覆纹、创生、黯星等环合公共行；公共行不重复角色名，伤害条和逐击点使用
角色颜色标明归属。援护技的 `环合·浊燃/黯星` 攻击类型描述触发关系，援护技自身的 QTE GE 仍属于角色
直伤；只有 `Buff_Reaction_*` 等明确环合 GE 才进入公共环合行。Core 个别逐击会复用残虹蚀心/鸩火 GE，
但正式 `damage_name` 已明确为“浊燃”；这种冲突只把原 GE 保留为来源证据，伤害渠道与重放身份按浊燃处理，
不再污染蚀心/鸩火层数。同一稳定 Ability、实际技能来源和伤害渠道的连续逐击才可合并，不同技能不得共用一条；
安魂曲习得技能以稳定伤害 GE 区分实际来源，不能只按外层 `GA_Lacrimosa_Skill` 合并。她的
`GE_Player_Lacrimosa_SwitchModB_Damage` 由 G 技能蓝图直接引用；虽然伤害表和既有逐击把它绑定为
`GA_Lacrimosa_Melee`/普攻，展示与动作推断仍采用已审计覆盖 `GA_Lacrimosa_SwitchSkill`/G 技能。
`GE_Player_Lacrimosa_AnHunZhouTwo_Damage` 则属于被动“番茄酱盛宴”的失谐追加伤害，不冒充 G 或普通攻击。
灵可同频合击保持五种身份分离。当前版本化公式推论暂按“队友是画面执行角色和倍率定义所有者、公式面板与
技能等级归灵可、伤害属性来自发起角色的发行静态角色属性”投影，这不是 DLL 或原生 `SourceAttributeComponent`
实证。E 入口要求完整有序四段 E、同目标和唯一响应关系：v5 优先由覆盖 QTE 或在其前 350ms 内结束的唯一
type6 选人区间选择 QTE；旧战报没有类型证据时，在静态响应窗口内取时间最早的唯一合法同目标队友 QTE，
同刻并列或多个 E 竞争同一 QTE 时保持未知。旧战报还能把 E 第四段末击到所选 QTE 静态动作起点之间的
正区间作为低置信选人暂停；静态起点不可用或不晚于 E4 时退到 QTE 首击。v5 已有 type6 时不重复累计，且任何
type6 都不能脱离完整 E 和唯一 QTE 单独证明触发。`QTE → Linko LTE AOE` 唯一配对可形成
低置信同频响应推论，但不据此命名 E/Q 触发来源。另有覆盖完整配对的 nte-core 记录时停时最多升到中置信；
Q 动作回退区间仍保持低置信，任何时停区间都不能单独证明触发。普通 QTE 不重标，Core 原始逐击归属、技能构成和动作泳道不改写。灵可弱点感应的 10%
通伤只投影到“有正式追加标签或版本化同频推论，且目标已触发覆纹”的逐击；精确调频按同一派生入口和发起角色属性建立目标 8%
减抗、12 秒区间，区间从触发同频 QTE 的首击时刻起且该首击本身消费减抗；同一目标的同属性只刷新、不叠加，
不同属性分别维护且每种属性同时至多一层。数值规则与
推论置信度分开记录在派生结果中，不写回 Core 原始逐击。属性边际按公式面板所有者纳入 E 响应和
`QTE → LTE` 两类同频 QTE；其面板变化也通过实际原伤害基数联动同事件覆纹，但覆纹环合强度仍读取原始执行
角色，不能用同一个公式面板身份覆盖。精确调频按减抗属性分别反事实分组，覆盖逐击只统计实际应用该属性修正的
公式投影，不能把同一时段内元素不匹配的逐击计作覆盖。
灵可觉醒的固定轴部分继续复用上述公式身份：觉醒二在普通 Q 爆发 GE 与强化 GE 间双向切换；三觉共鸣的
`QTE/E/Q +1` 与自身攻击提升作用于灵可面板和技能等级，因此同频 QTE 也会随之变化；觉醒六的暴击率
`+25%` 只投影到同频合击公式消费者。六觉共鸣由 Q 开始提供 13 秒有效战斗时间的全队灵/咒属性伤害
`+30%`，记录时停不消耗持续时间。改变攻击间隔、重置冷却、额外生成同频范围伤害、追加环合反应和回复
资源的部分会改变原固定轴动作或命中，当前保持未量化，不用零收益冒充已计算。
技能条粗细按整组
伤害缩放，圆点大小按单击伤害缩放；悬停显示短摘要，重叠候选可重复点击轮换。左键点击逐击点打开可复制的
非模态公式弹窗，不在时间轴下方常驻详情区。弹窗展示实际伤害、采用的预计伤害、按
`(预计-实际)/实际` 定义的有符号误差、预计非暴击值乘暴击期望区得到的预计期望，以及按同一误差同比补正的
实际期望；正误差表示高估，负误差表示低估。详情同时展示公式类型、推断暴击、完整乘法链、每个乘区的符号
公式、来源项和数值代入，以及人物/弧盘/好感度/装备/具体 Buff 等可寻址来源。该结构化来源由重放结果拥有，
界面只负责格式化，后续边际计算可直接消费，不解析展示文本。悬停摘要和点击后的完整公式弹窗都显示原始
逐击 `event_id`，便于回查逐击日志和原始轴，不另造展示主键。同 GE 高低数值对只能标为弱证据补正，不冒充
nte-core 暴击标记；噩梦、浊燃等具有离散层数乘区的伤害不参与该配对，避免把层数倍数误判为暴击倍率。
左键查看详情不选择或高亮其引用伤害，也不跳转、筛选逐击日志。

统一轴默认展示“包含时停”的实际经过时间，也可切换为“扣除时停”的有效战斗时间；所选口径统一驱动轴刻度、
日志时间、区间输入、时长和 DPS。页面上方的队伍 DPS 与战斗时长固定复用同一扣时停投影，战斗时长括号内
保留包含时停的实际经过时间；nte-core 记录区间与 Q 动作低置信回退不得在汇总卡另写一套扣减算法。横向缩放提供
10%、25%、50%、100%、200% 至 800% 固定档位，默认 100%；100%
使用固定时间密度，约等于旧 400% 的视觉间距，画布按当前口径下的整场时长计算宽度。计算宽度小于可视区域时
自动铺满并展示整场，放大或缩小时保持视口中心对应的战斗时间。横向命中按当前显示时间的毫秒容差判断，不以缩放后的
固定像素距离替代时间语义；点和瞬时动作的视觉最小宽度会先换算为当前口径下的显示时间区间，保证整个可见
对象都能悬停和点击。所有泳道在页面内完整展开，不设置内部纵向滚动，只保留一条横向滚动条；左侧泳道说明列
覆盖在滚动画布之上并固定于视口左侧，角色头像、直伤/动作说明和公共伤害行名不随横向滚动。拖拽
画布会平移横向视口；布局按分析对象、时间口径和画布宽度缓存，重绘只处理当前横向视口附近的输入、动作、
伤害条和逐击点。输入块、动作条和伤害条继续使用悬停摘要，点击逐击点打开完整伤害公式弹窗。右键菜单可把所在时刻
设为分析开始或结束，数值输入仍可精确应用时段。主页面的开始/结束轴秒、时间口径和缩放，以及边际子页面
的角色选择和手工属性输入都忽略鼠标滚轮，滚动浏览页面不会改变当前选择。选定时段角色贡献同时展示逐角色
伤害、占比条和环形占比图。
其后复用统一伤害构成卡片，并提供“粗分 / 细分”两档。每个角色卡标题在总伤害右侧显示其占团队总伤害的
百分比；每个构成行同时显示“占本块伤害”和“占团队总伤害”两个百分比。粗分把角色正式逐击归为直伤、持续伤害、附着物、
该角色自己的倾陷贡献、生命上限结算、共享伤害和每一种命名环合；浊燃同时具备持续伤害和环合身份，但在
构成中只计一次并优先显示为“环合·浊燃”。噩梦、蚀心、鸩火、白藏 Q 领域和阿德勒 E 后续属于持续伤害；
九原“致命玫约/致约清算”属于附着物，“风声为我所用·追加清算”属于追加直伤。只有
`GE_Player_Kuhara_Seed_Damage`、`GE_Player_Kuhara_BudBoom_Damage`、
`GE_Player_Kuhara_BudEnd_Damage` 和 `GE_Player_Kuhara_SeedReaction_Damage` 接入九原正式独立公式：固定自然属性、攻击缩放、角色暴击，并正常消费攻击、通伤/
自然伤、防御、自然抗/穿透、暴击率和暴伤；它们不继承某条触发直伤，其他 attachment 不因此泛化。细分把
其中 `GE_Player_Kuhara_SeedReaction_Damage` 以 Core 的 `GA_Kuhara_Passive_2 / Passive Damage`
作为被动身份，不读取静态伤害表关联的 A 技能等级；静态来源类别仍原样保留用于审计。
同一角色的普通攻击各段合并为 A、原生战技各段合并为 E、原生终结技各段合并为 Q；QTE、G、闪避反击、
格挡反击和被动各自保持独立。每一种命名环合、DOT/持续直伤、附着物、共享伤害、倾陷和生命上限结算继续
按正式机制身份单独聚合，不吸收到 A/E/Q，也不把不同机制合在一起。
团队倾陷逐击不使用封包中的触发角色作为伤害所有者；加载逐击公式后，按每名出场角色的等级、倾陷强度、
属性、防御/抗性和命中时 Buff 计算贡献比例，再按该比例拆分本次实测倾陷总伤害，因此各角色小计之和始终
等于实测值。尚未加载公式时显示“倾陷归属待计算”；缺少明确目标或公式证据时显示“倾陷归属证据不足”，
两者都暂列未归因而不冒充触发者伤害。飞弹反射保留“机制伤害”身份且暂不接入公式；Core 已明确给出
`character_id` 时仍归入该角色的“敌方飞弹反射伤害”，只有角色证据缺失时才进入“系统 / 环境”。环境、
载具等没有角色所有者的伤害也进入该独立卡；最底部“未归因”只保留无法确定角色或渠道的逐击、生命结算和
总额差异。“未知角色”或“未归因”没有正数内容时不渲染空区块。Ability、GE、
伤害项、组件和攻击类型全部缺失的逐击明确显示“来源字段缺失”，即使协议给出了角色也不计入该角色技能；
完整且正常归属
的战报应为 0。上方角色贡献表和环形图使用同一归属结果，不与构成卡发生倾陷总额矛盾。只有聚合摘要的
旧战报继续按当前/上下半场渲染原构成卡片，不把两种统计口径混用。筛选和日志分页不改变统计范围；
上一页/下一页显式改变日志页码并复位表格内部视口，同时保持外层长页滚动位置。最近分析
范围和角色选择属于最小页面状态，不改写战报事实。时间轴 Canvas 的泳道、命中、拖拽、缩放和绘制 contract
与审计弹窗、边际子页面相互独立，页面布局调整不改变轴上事件。

完整的 v4 最终轴提供正数结构化 `max_hp_reduction` 时，`BattleTargetVitalAnalysisService` 直接采用 Core 最终
generation 上的校准值，最大生命样本仅用于一致性校验，不能重复生成生命上限结算。结构化零值通常阻止样本
回退；当前 Core 尚未覆盖法帝娅黯星的观测差值语义，因此只有有效养成已解锁“罪感熔炉”、同半场同目标在
`Buff_Reaction_4_new` 后五秒内出现单调 HPMax 下降时，才允许一次法帝娅定向样本回退。其他零值下降不计入
正式伤害，只推进后续已知机制使用的单调基线，不能累计归给法帝娅。字段缺失、旧 contract 或不完整轴存在
正式目标最大生命样本时，Service 按目标维护单调下降的已确认最大生命，忽略
回跳的陈旧样本，并把每次下降投影为可重算的 `BattleMaxHpReductionEvent`。结算前当前生命取变化前同半场、
同实例、同旧最大生命的附近连续逐击中最小可靠 `HPAfter`；结算伤害固定为
`观测最大生命下降 × 结算前当前生命 / 下降前最大生命`。下降后首行的 `HPBefore` 已是结算后样本时，该行
正式伤害仍直接使用 `HPBefore - HPAfter`，不再减一次上限结算；页面另列生命上限结算和
两者之和的有效伤害。派生事件进入同一左闭右开分析时段、角色贡献、伤害构成、技能明细、目标证据和时间轴
“生命上限结算”公共行。当前只对两种已审计机制做角色归因：有效养成激活 `Effect5` 且附近存在噩梦 GE 时
归属于安魂曲五觉；附近存在法帝娅 `Buff_Reaction_4_new` 时优先归属于法帝娅被动。归因使用静态规则与附近
事件，数值始终使用目标实测变化；静态描述倍率不覆盖观测差值。其他最大生命下降仍保留为未归因事件，
归因置信度与结算置信度分别展示。完整原始轴精确出现角色 1004 的
`GE_Player_Lacrimosa_Blood_Damage_LV6` 时，另生成高置信、带证据事件与算法版本的安魂曲五觉生效推断事实；
它不改写冻结觉醒或默认勾选角色页。达芙蒂尔的 `GE_Player_Daffodill_ExtraUnbalance_Damage` 只作为本人额外
倾陷公式锚点，不证明五觉；零觉战报保持零觉且洞察本身已有一次额外结算。边际候选明确勾选五觉时，分析才
按原轴 Q 的目标、洞察层数和既有倾陷时点每层再增加一次候选派生结算，即一层总计两次、三觉叠至二层时
总计三次，并计入角色、全队、时间轴和伤害构成收益，不写回原始逐击。边际页在指标与
时间轴之间单列“候选新增机制结算”，展示原轴触发逐击、洞察层数、单次公式值、新增合计以及角色/全队收益；
派生行可双击打开完整公式。点击原始倾陷行时先完整显示团队逐角色求和公式，再追加关联的五觉个人倾陷公式，
同时分别展示实伤固定轴结算簇与候选公式审计合计；额外结算不得替换或隐藏其他角色的团队倾陷贡献。
目标身份在正式逐击和派生事件中使用同一个解析器：`target_id` 完整时按 `(半场, target_id)` 分实例维护；
部分行缺失实例 ID 时，缺失行不参与最大生命派生。整场都没有实例 ID 时，生命证据可保留显式“单目标假设”
或“用户确认单目标”分组，但该假设不冒充 nte-core 实例，也不作为新逐击敌方属性的主要目标回退。“主要
展示对象”只负责环境编辑器默认展示和旧记录兼容。

未保存条件时，分析层始终使用完整遭遇或完整半场证据，不受 UI 时间范围、缩放和逐击筛选影响。它按
`(scope_half, target_id)` 汇总每个正式实例；轨外使用 `upper/lower`，非轨外把半场归一为空。Core v4 初始
最大生命取同实例完整轴中正数 `target_max_hp` 的最大值；同一行的 `max_hp_reduction` 是该击之后结算的生命
上限变化，只用于生命事件和后续上限下降校验，不能加回本行结算前的 `target_max_hp`。只出现一次、缺少
target context、且与 5ms 内另一正式实例拥有完全相同最大生命、HP 前后值和总伤害的行，仍只在派生观测中
视为该正式实例的重复别名；原始逐击完整保留。

自动目录在候选加载阶段冻结每个怪物槽位的生命、防御、倾陷和抗性画像，再以 1 点绝对 HP 容差、Core 正式
怪物 ID、玩法、轨外层和半场构建“观测实例 → 静态槽位”二分图。所有观测实例必须一对一注入不同槽位；相同
HP 的两个实例会消耗两个槽位。当前没有 `target_roster_complete` 正式证据，观测数量只作为下界，因此仍可
保留含未命中静态槽位的候选。严格池为空时环境和目标保持未知；非空时始终按正式身份命中数、较少未观测
槽位、当前/正式目录顺序和稳定 `environment_ref` 选择一个默认环境。候选不唯一时保留全部 alternatives，
输出低/中置信和默认原因，不宣称环境唯一。候选公式画像不等价时，分析层在整场冻结逐击轴和原始战报
build 上分别重放，不消费 UI 时间范围、战报养成编辑、边际候选或禁用推断事实；按角色、伤害项、属性、
半场、目标实例、Buff 指纹和 DOT 状态段分组，只用观测伤害反推暴击/层数之前的
`non_critical_damage`、`critical_damage` 与 `expected_damage` 的对数 Student-t 鲁棒残差选择胜者；
`corrected_expected_damage` 不进入评分。只要严格池
非空就采用稳定胜者的防御和抗性参与公式，即使证据少或残差接近也返回低置信结果，同时保留冲突候选并在
当前范围以红字提示；当前没有 Bootstrap 的低/中置信残差胜者只写公式画像和稳定显示 ID，不写成已解析
怪物身份。争锋冲突的当前范围标题只显示“候选冲突：争锋赏宴 · 对象 · 本场初始最大生命最高的怪物名”，
难度、加成、置信度和残差证据不重复塞入标题，仍由环境配置和证据提示承载。当前范围同时覆盖轨外上下半时，两半使用完整证据各自缩池；只有
默认结果属于同一个 `AbyssID` 才合并赛季环境，敌方实例画像仍按半场隔离。所有派生只存在于当前分析投影，
不写回原始逐击，也不覆盖 Core `target_monster_id`。
具有可靠正式 `target_id` 的逐击会按“半场 + 目标实例”分别维护最低 `HPAfter` 前沿；整场缺少正式实例 ID 时，用户
确认唯一目标后也使用同一规则。逐击从陈旧 `HPBefore` 出发并与既有生命区间相交时只记录疑似重叠诊断，
不据此扣减正式伤害；原始 `damage/HPBefore/HPAfter`、角色/技能构成和主摘要保持不变。同一时刻的不同目标、
上下半复用的 wire ID 均互不串联。最大生命基线与附近机制归因也使用同一分桶。安魂曲五觉的 200% 描述只
用于归因和缺失实测时的描述预计；只要噩梦行满足 `damage = HPBefore - HPAfter`，就保留该正式逐击，不按随后
最大生命下降反推或改写本体。按生命比例计算的上限损失作为独立事件只增加一次；主摘要始终显示 nte-core 原始伤害，任何并发
逐击都不因血量区间相交而被删除或减值。目标证据表按半场和实例分别显示观测耗血、分析有效伤害与有符号
未解释差额，并以“观测耗血 = 分析有效伤害 + 未解释差额”严格闭合；差额不分摊给角色或技能。
同一目标 700ms 内不同伤害项若上报相同伤害，且 HP 区间相交或结算端点一致，两条逐击只增加“伤害归属污染”
审计标记；时间距离本身不触发。系统保留各自原始伤害与公式候选，但不让它们参与暴击或 DOT 层数反推。

统一时间轴第二行提供环境配置入口，并以当前对象名或“未知”概括范围；已配置、已推理和未配置的证据说明
放在按钮提示中。具体选择器和敌方属性编辑位于独立弹窗，目标/生命证据表不承担配置入口。发行静态目录提供四种环境分流：
大世界按官方图鉴区分普通、精英和异象追猎，可多选、指定主要展示对象并选择其正式世界等级/属性档；材料/养成副本按官方类目、活动、
难度和刷怪模板逐级选择；轨外之境只展示由大陆服生效时间确定的当前与下一配置，可选择层数和上下半场；完整上下半战报手选任一半场后，自动冻结同配置同层的两半目标画像，
`AbyssID` 是整套配置 ID，`AbyssLevel` 才是层数；争锋赏宴可选择 8 个挑战对象、4 档难度及每组挑战加成。
自动反向识别只搜索当前/下一轨外配置、争锋全部八个挑战对象、具有逐难度正式怪物池的高危委托、经验及甲硬币/异能升级材料/弧盘突破材料/空幕
四类副本，以及异象追猎单 Boss 的世界等级变体；仅有通用怪物池的旧高危委托、异象巡礼、普通大世界怪群及其他活动只能人工选择。
已绑定属性包的对象直接带入等级、
`DefBase/DefUp/DefAdd`、`UnbalMax` 和普通、暗、光、咒、相、灵、魂、心灵八类基础抗性；只确认刷怪模板
而未闭合属性包的副本对象保留人工补正，
不按中文名猜数值。争锋唯一命中时，实测最大生命决定生命加成档，攻击与每个抗性类目默认选最高档，计时
选项保持未推断；抗性加成叠加到对应战前抗性，所有默认值都可在保存前由用户修改。保存同时冻结对象集合、
当前展示对象、难度、选项，以及每个候选/刷怪槽位的最大生命、等级、防御、倾陷上限和分属性抗性。已保存
环境使用持久冻结画像；v35 以前已经保存环境但没有逐目标画像的单目标记录，在首次分析时用已保存的敌方
条件和实例实测初始生命派生一次性内存画像，正式目录只按需加载用于恢复怪物显示名，不自动回写历史条件。
用户已保存条件时跳过自动环境目录推断；未保存条件时，默认环境在候选加载阶段已冻结画像。两条路径都用
完整轴的初始最大生命与 `(半场, target_id)` 一次建立统一实例映射：身份唯一时得到对应怪物 ID
和档案，`default_monster_id` 等于唯一的 `resolved_monster_id`；多个怪物 ID 乘区完全相同时，
`resolved_monster_id` 保持空、`possible_monster_ids` 保留全部候选，逐击直接共用冻结档案，并派生一个只供
默认名称和审计定位的 `default_monster_id`。当前 v35 冻结档案没有可靠正式目录排序字段，因此 default 按
“ID 大小写归一排序、原 ID 稳定兜底”的规则选择，不依赖候选输入顺序；它不写入 v35 冻结战报或用户确认
条件，只随完整自动推断 payload 写入账号 v38 的可删除重算派生快照。恢复时直接消费快照内冻结画像，不以
default 重新查询静态库，也不覆盖 Core `target_monster_id`。乘区不同或档案不可用时 default 留空，逐击保持
未知且不回退主要目标。自动环境存在 alternatives 时也只映射稳定默认环境内的槽位；默认环境不能强选其内部
具有不同公式画像的同 HP 怪物。映射随分析快照一次生成，逐击公式按需加载时只做内存查表，不逐击访问静态库。该
条件不改写逐击或目标生命证据，删除战报时随记录级联删除。
用户已确认的环境仍先按半场和静态数量做完整映射；wire 实例数量与静态刷怪数量冲突时，才允许各实例按正式怪物
身份和初始最大生命独立匹配冻结画像。同 HP 对应多个不同公式画像时继续未知，不回退到主要展示对象。
轨外自动识别先把战报采集时间从 UTC 换算为大陆服时间，并用正式轮换起止区间唯一确定赛季；玩家看到的候选名
同时展示赛季中文名和月日区间。时间确定赛季后，层数、半场、HP 和身份只负责匹配该赛季内部目标。完整上下半中
若一半严格匹配、另一半目标多重集冲突，分析保留已确定赛季和已匹配半场画像，未匹配半场继续未知，不再把整个
环境显示为跨期候选冲突。缺少可解析战报时间时不取应用当前时间，仍按冻结目录顺序低置信回退。
轨外关卡展示的“怪物弱点”是上/下半的推荐属性数组，底层已直接反映为对应敌方属性包中较低的
基础抗性，不再叠加独立弱点 Buff。`Abyss_8` 第 12 站上半光/相为 10%、其他常规属性为 30%；
下半暗/咒为 10%、其他常规属性为 30%。

同一弹窗可为本场选择一项魔女赐福；下拉框只展示可计算效果名，不拼接剧情说明。可选值为攻击力 15%、
暴击率 10%、暴击伤害 20%、通伤 15%、蓄质速率 20%、
倾陷强度 60 或原质驱力 60。选择值来自静态曲线并作为用户确认的整场全队 Buff 投影；历史战报不会读取
“今日赐福”覆盖当时选择。已映射的伤害属性直接进入逐击公式和边际，蓄质速率等尚不属于当前伤害乘区的
属性只保留选择与证据，不伪造伤害收益。

静态 Buff 的有限持续时间统一按扣除时停的有效战斗时钟推进；残虹、安魂曲等角色在 Q 时停前后触发的效果
会在原始轴上顺延，时停本身不消耗持续时间。显式移除事件仍按正式发生时刻终止区间。

已建模技能触发没有对应实测最大生命下降时，目标生命 Service 还会生成独立的“描述预计”事件作为弱证据。
安魂曲五觉按本次噩梦伤害的 200% 预计最大生命削减；法帝娅被动按每次黯星结算时来源当前 `MAXHP` 的
200% 预计单次目标损失，并以全队每次获得来源当前 `MAXHP` 的 10%、最多 5 次作为保护边界。两者再乘当前目标生命比例得到
预计有效生命损失。实测事件的证据引用覆盖到相同触发时，删除对应描述预计，避免重复计算。“描述预计”有
独立时间轴行、目标事件行和合计值，固定标记为低置信，默认不进入有效伤害、角色贡献、伤害构成或反事实
边际。缺少本次实测下降时，来源当前 `MAXHP` 从冻结 `PanelHP`、三觉的固有生命固定加成与前序本机制层数
递推；正式黯星结算只从该击之后增加一层全队 `HPMaxAdd`。若本场已观测到归因明确的目标最大生命下降，
则以该事件的 `下降值 ÷ 200%` 反推本次来源当前 `MAXHP`，只校准该次及后续递推，不从战斗 0 秒补写面板。
层数由本场正式黯星逐击推进，等待未来运行时 Buff 证据覆盖。

开始采集时只固定账号 generation 并暂存逐击。结束后以逐击和摘要中实际出现的官方角色 ID 为范围，读取
当时最新一份完整 `nte_core` 背包中的游戏当前装备，并读取这些角色当时角色页有效养成，只物化相同人数的
人物等级/突破阶段、弧盘等级/突破阶段、好感度 10 级开关、具体觉醒选择、技能、官方静态额外形状、空幕和
驱动；不读取配装槽位。账号没有完整原生背包时，仍保存摘要、record 和逐击，并从发行毕业模板冻结该场
角色的假定空幕、驱动、套装和全部词条，按同一公式冻结计算面板；聚合驱动按模板空幕套装所需形状假定齐套，
供已有套装效果模型判定使用。保存提示和配装来源标明“毕业模板假定”。
假定装备以独立来源元数据随角色快照保存，不创建背包快照或正式 UID。该基准支持逐击重放、属性收益、
空幕主属性比较和养成修改；聚合模板装备只读，不支持逐件替换或游戏装配。导出与导入保留假定来源和词条，
后来收到背包或重试保存不会替换该场基准。schema v25 还按
同一角色页公式冻结基础/百分比/固定属性组成、暴击、通用与角色属性增伤、环合/倾陷强度，并记录伤害模型和
名称映射版本。物化完成后战报副本独立于原背包快照；v23/v24 旧记录只能按当前静态库尽力重建属性并明确
标记来源。旧战报或修改副本缺少官方额外形状字段时，分析层按当前静态角色 ID 在内存重建，不改写原始快照；
账号私有修改副本可以通过维护工具按静态库回填。角色页没有显式账号配置时，毕业模板只提供配装、弧盘和技能等兜底；觉醒遵守角色页默认未选中，
不能把毕业模板中的满觉理论条件冻结为用户实际觉醒。历史记录若明确标记为 `official_graduation` 来源，也在
重放时按同一零觉默认归一化，用户保存的战报修改副本优先于该兜底。

原始角色配置与本场空幕/驱动保持不可变。本机采集战报每场最多维护一个完整的用户修改副本：尚无修改副本时，
首次编辑必须从本场原始冻结养成和配装复制草稿，不得读取当前角色页替换其初值；已有修改副本时继续加载并
原位覆盖同一副本，不产生多个版本。只有用户显式执行“从角色页面同步”时，当前账号角色页才覆盖修改副本，
原始快照始终不变。“恢复原始快照”只停用副本，不删除内容，因此仍可继续修改或重新启用。外层编辑器可选择
本场原始、游戏当前或已保存槽位作为配装来源并替换装备；保存只把装备完整复制到本场副本，不修改角色页、
库存、方案或游戏。导出若发现已保存配装覆盖，始终以该校准装备为准，同时保留副本当前是否启用的指针。

边际计算使用战报页内带返回按钮的独立子页面，不使用弹窗；角色
切换只懒加载当前角色编辑器，避免一次渲染整队全部装备卡片。编辑器复用角色页的基础、觉醒、技能、弧盘和
空幕/驱动装备卡片，顶部用一行候选摘要展示人物及弧盘临界等级的突破前后、觉醒、好感、弧盘精炼、技能
等级和空幕/驱动数量。新战报原始快照和修改副本都显式冻结弧盘突破阶段；旧战报该字段保持 `NULL`，分析时
按旧等级语义派生，只有显式保存修改副本或同步到角色页时才写入新状态，绝不改写原始快照。
配装头部仍按角色页统一评分规则汇总总分及固定区间评级。角色页的面板直伤公式和只读词条权重属于分析
结果，不在战报副本重复展示。
外层编辑中每个角色可以选择本场原始、游戏当前或已保存槽位作为本场校准配装。保存时复制所选
上下文当下用于计算的装备、词条和正式 UID，不保存背包或槽位指针，也不允许理论最优或虚拟补位；角色页
来源沿用角色页满级计算口径，本场原始来源沿用战报实测冻结值。副本一旦启用，就成为该战报全部派生分析
的当前权威配置；分析 Service 使用其养成与所选配装重新计算面板、Buff、逐击重放和边际基线，原始冻结
快照只保留审计、恢复和重建副本固定轴的意义。旧副本没有配装覆盖时仍使用本场原始装备。主分析页的
“编辑角色”位于“当前范围”左侧；弹窗底部提供取消、两种角色页导入、保存副本和
仅养成写回五个动作。“保存并同步到角色页（不含空幕驱动）”只写养成字段，不写战报装备、位置、UID 或
手工战报属性；单纯保存副本也不改变角色页。“从角色页面同步（不含空幕驱动）”读取当前角色页养成，覆盖
并启用本场唯一修改副本，同时保留副本已选配装；“从角色页面同步（含空幕驱动）”还把角色页游戏当前
空幕/驱动复制为本场独立配装覆盖。两种导入都不写角色页，原始快照始终不变。

固定轴边际冻结本场动作、逐击、目标、Buff 时段和分析区间，允许选择实际出场角色，并调整整队角色等级、
好感、觉醒、技能和弧盘；本机采集战报还可临时替换空幕和驱动。进入边际页时，替换候选读取一次当前账号的
最新稳定背包快照并冻结到本次页面会话，不使用战报结束时的旧背包，也不因角色没有已保存方案而变成空候选池；
页面打开后发生的新一轮背包同步不追改正在编辑的候选。页面不再提供手工面板覆盖，属性单位表只按
实际可刷出的金色驱动副词条目录计算每格词条对当前公式面板和当前队伍的期望边际，不补列元素伤害、属性穿透、
防御忽略等非驱动副词条。暴击状态无法确定的普通直伤按角色
暴击率计算期望；固定 50% 暴击率的浊燃、蚀心和鸩火不消费角色 `CritBase`，但仍消费暴击伤害。
“量化率”是当前公式面板实际应用本次变化比值的伤害占该面板关联伤害的比例；已证明不受该属性影响的伤害不计入
分子，因此它既不是抓包完整率，也不是结果可信度。`complete`、`partial`、`unavailable` 和
`not_applicable` 同时约束公式输入和状态证据：公式乘区全部可算、但实际 Buff 或目标状态只有中低置信推断时，
仍以 `partial` 展示，并保留“该状态成立时”的条件数值；不得因公式可算就冒充状态已实测。二者不能互相替代。
“伤害占比”仍是当前角色总伤害占
全队总伤害的比例；当公式面板控制了 Core 归到其他角色名下的伤害时，面板关联伤害可以大于该角色的原始
归属伤害，但不能大于全队伤害。当前启用的已保存修改副本先从不可变原始快照物化为当前生效固定轴；进入边际页而未
修改草稿时，显示该固定轴本身且收益严格为 `0`。之后的内存草稿只与这份当前生效固定轴比较，原始冻结
快照不得再作为用户可见收益的分母。属性单位逐击锚点、角色分母和团队分母均消费最终候选投影，不把原战
逐击再乘一次副本或草稿倍率。候选分母为 `partial` 或 `unavailable` 时，属性结果继承该状态，不显示完整角色或全队
收益。倾陷强度使用已有完整队伍倾陷公式。候选只保存在当前页面内存，不调用战报副本保存，
离开边际页、切换战报或重新打开后丢弃，也不进入导出。其基线为当前启用的外层副本，否则为原始冻结快照；
所选角色决定重放半场。倾陷公式已保留逐角色贡献格时，省略的零值 `UnbalIntensityBase` 仍表示可从零增加的
正式基准，不得判成不适用；但默认属性单位集合仍以金色驱动副词条目录为唯一边界，不因角色伤害属性额外补充
元素增伤或穿透候选。
倾陷封包归属不参与通用逐击比值，当前角色桶只消费伤害构成使用的该角色倾陷贡献，本角色收益始终以本角色
面板关联伤害为分母，全队外溢增量另以当前团队总伤聚合，所有量化桶必须严格回收到该面板关联分母。覆纹追击只在
同一正式 sequence、半场、目标和方向存在被记录原伤害时，复用该原伤害实际来源角色的攻击/生命/防御缩放、
暴击、通伤、属性伤、防忽和属性穿透比；正式 DOT 等来源也可参与；`MagBase` 的基础面板与条件 Buff 均读取
该原伤害来源角色，并修改覆纹自身追加倍率区。页面预投影与候选批处理沿用同一来源，不混用覆纹声明角色的 Buff。
逐击 Buff 详情保留原始归属口径，公式详情消费实际公式来源口径；两者读取本次原生分析返回的共享投影与决策，
点击时只渲染既有结果。候选展示轴使用对应候选的投影，缺少详情时显示未生成，不在界面重算 Buff 规则。
缺少这条同事件关联时保持未量化，不按 QTE 触发者、环合双方较高值或相邻逐击猜测来源。环合强度只消费
逐击重放中已保存的正式公式因子：覆纹保留本击实际 20%/灵可 30% 分支，创生、浊燃与黯星使用各自的通用
环合强度区。浸染正式公式已确认是 QTE 后 12 秒团队最终增伤，且每击读取该击实际伤害来源角色的环合强度；
生产固定轴消费者接入前仍不进入边际，不把公式已知误写成生产已量化。

每条逐击按 `(scope_half, target_id)` 消费分析开始时冻结的目标实例画像。身份未知但画像唯一或所有身份候选
画像等价时，防御、抗性等目标乘区仍可完整计算；画像未知时，攻击、通伤等只改变角色侧乘区的边际仍可将
前后不变的未知目标乘区相消。防御忽略、抗性穿透、敌方减抗或目标状态等发生变化且缺少相应画像时，只把
已有输入支持的分量标为 `partial`，完全不能量化时标为 `unavailable`；只有已经证明本次变化不作用于该击
才标为 `not_applicable`。`partial` 的数值不是完整收益，也不默认是收益下限；未知不得显示成 `0%`。

点击“重算”后，只有所有相关变化均完整量化时才展示完整候选队伍 DPS、候选总伤害和总收益；部分量化时
改为展示“已量化变化下”的伤害与收益，并列出缺失依赖；不可量化时显示 `—`。候选未调整时可以证明无变化，
收益为精确零。结构化逐击使用候选与基线的理论期望公式比值，再乘原始实测逐击伤害；无论原击识别为暴击、
未暴击或分支不确定，均按各自正式暴击策略比较期望，不冻结本场暴击结果。属性单位边际以当前候选逐击伤害
为锚，暴击乘区按 `(1 + 候选暴击率 × 候选暴伤) / (1 + 当前暴击率 × 当前暴伤)` 逐击缩放，暴击率读取命中
时的动态加成并限制在 0%—100%。暴击率与暴伤同时改变时只计算一次联合比值；不可暴击伤害不获双暴收益，
未知策略保持未量化。历史实测伤害与逐击暴击识别不因收益计算而改写。peer 降级只允许在公式家族和因果依赖
一致的逐击间复用；任何目标敏感
比值都必须保持相同 `(scope_half, target_id)`，不得跨目标、跨半场或用主要目标兜底。固定轴仍保留所有原始
逐击；未量化逐击可以原值占位，但不计入完整候选收益，也不能把占位解释成 `ratio=1`。

边际子页面还使用与主页面相同的时间轴 Canvas 展示调整后逐击轴：动作、输入、命中时间、泳道和选定范围
保持不变，技能条粗细、动作汇总伤害和逐击点大小按候选伤害重新投影。时间轴上任何已经展示的逐击点都必须
可打开详情：尚未重算时展示原始逐击与原始公式，重算后再增加调整后预计伤害、有符号预计提升/下降、原始/
候选结构化公式值、估计方法和候选配置公式；部分或未量化的原轴占位也必须打开并说明缺失依赖，不得因为
没有候选行而静默无响应。该变化不称为“计算误差”。已归因生命上限结算继续使用来源角色的颜色和泳道归属：
安魂曲五觉已有实测结算时，以实测有效伤害为锚，按对应正式噩梦 GE 逐击的候选/原始伤害比联动，不从邻接
生命样本、结算行、普通直伤或不完整轴重建另一份绝对血量；比值为 1 时必须严格保持实测值。候选新勾选五觉
但没有实测下降时，按 `候选噩梦伤害 × 200% × 结算时生命比例` 新增候选伤害；生命比例缺失时使用 50% 期望，
仍计入角色和全队候选收益并明确标记估算。法帝娅最大生命转移按候选/原始冻结来源当前 MAXHP 比联动；共享
伤害因实际承伤链不足保持未量化。机制被关闭时归零，证据不足时以原值保留轴上事实但标为未投影。


边际页顶部把返回、分析角色、当前改动摘要、重置、重算和帮助放在滚动区域外的一条固定工具栏；分析角色使用
强调样式，长页滚动时工具栏不覆盖第二行内容。“驱动副词条单位边际”上方展示当前角色的横向人物面板：表头
完整列出基础、额外、百分比及总攻击/生命/防御、暴击、暴伤、通伤、本系和其他元素伤害、环合、倾陷、防忽、
各元素穿透、治疗、护盾与充能等冻结属性，第一行是静态面板，第二行是按公式面板关联伤害加权的动态面板；
本系只按该角色自己造成的非反应、非覆纹逐击属性判定，无法判定时不猜。无公式关联或动态证据不足的属性显示
未知，不把静态值伪装成动态值；表格允许横向滚动，快照中目录外的属性继续追加展示。

属性单位表展示属性单位、面板属性、伤害加权当前面板属性、面板关联收益、全队期望收益、关联面板伤害、面板
关联伤害和关联全队伤害。加权属性按与该属性关联的公式面板伤害加权；逐击使用命中时公式面板加动态 Buff 的
有效属性，目标减抗不伪装成角色穿透；联动生命上限结算继承来源逐击属性，倾陷只使用归属于该角色的贡献。
原始角色归属伤害加由该面板控制的外部逐击构成面板关联分母，原始伤害归属和全队分母不改写。

属性单位表之后提供空幕主属性反事实。当前角色只需存在一件可识别 `core`，蓝、紫、金等品质均可作为“当前”
配装；候选主属性统一读取官方金色空幕 20 级曲线，不沿用当前空幕的品质、等级或主属性数值。每个候选固定当前
空幕的套装、副词条和其他字段，分别展示相对“移除当前主属性”的贡献以及“用该金色主属性替换当前主属性”的
实际换装收益；当前属性同类但数值未达到金色满级时仍必须重放，不能直接显示零。空幕主属性和配装重算共用的
固定轴比较中，覆纹候选伤害必须同时乘配对原伤害的候选/基线比与覆纹自身倍率的候选/基线比；配对来源无法
量化时，覆纹必须继承部分或不可用状态，不能仅按自身环合倍率重放后标为完整。角色有效面板必须保留全部元素
增伤字段；候选空幕即使不是角色本系元素，也要进入反事实面板和固定轴重放，不能在面板归一化阶段静默丢弃。

弧盘收益按同一战报固定轴建立三态：A 为移除弧盘，B 为移除弧盘技能/机制但恢复当前弧盘带来的常驻面板，C 为
当前完整弧盘。常驻收益为 `B-A`，技能/机制收益为 `C-B`，综合收益为 `C-A`，并展示 A 态预计角色与全队伤害。
精炼提供的无条件常驻攻击、生命、暴击或元素增伤只通过角色静态面板进入一次；弧盘战斗规则只投影条件或动态
效果，不能再重复生成同值的全轴 Buff。
若任一变化只有部分量化，三项分别继承对应状态和缺失依赖；只有数值均可用时才给出 A/B/C 加法闭合差。角色编辑器
内旧的弧盘“直伤收益”在战报边际页隐藏，避免与固定轴综合收益混用。下方团队 Buff 边际仍保留为逐 Buff 机制明细，
不与弧盘常驻、技能或综合收益重复相加。

边际页只对当前选中角色产生的 Buff 逐个独立移除，并按实际伤害接收角色展示“来源角色 → Buff → 受益角色”。每个受益角色
分别显示获得伤害、以该角色无 Buff 伤害为分母的自身提升，以及以全队无 Buff 伤害为分母的全队贡献；同一行
另列该 Buff 的全队额外伤害和全队收益。角色伤害覆盖率按该 Buff 确认覆盖的原始逐击伤害除以该角色原始伤害，
团队伤害覆盖率按确认覆盖伤害除以当前范围团队原始伤害；作用域或逐击角色未知的伤害单列为未判定，不并入
确认覆盖率。两项伤害覆盖均不等同于公式量化率。逐受益角色收益与无法归因部分严格回收到全队总量；不同 Buff 是在
完整当前配置中各自独立移除的反事实，不能直接相加。

固定轴不会凭配置变化生成或删除动作、命中和技能循环；未知特殊机制的轴外变化保持未量化，不按零增量估计。
直伤复用统一攻击、暴击、增伤、防御、抗性、易伤和独立乘区；静态库没有独立 TRUE 抗性，`TRUE` 不能自动
取消防御/抗性或读取虚构抗性，实际消费项由逐击 replay/channel 的正式公式因子决定。团队倾陷与达芙蒂尔
额外倾陷通过专用 channel 把 TRUE 外壳还原为逐角色固有属性，正常读取防御及对应元素抗性/穿透；其他缺少
专用覆盖的 TRUE 渠道保持未量化。分属性增伤只作用于该击实际属性。敌方飞弹反射明确
不纳入属性边际并计为未量化渠道，不猜来源联动。逐击没有正式
暴击标记时，暴击率与暴击伤害单位边际仍消费该击结构化暴击策略的理论期望；策略未知时保持原值并计入未量化覆盖。
噩梦、蚀心、鸩火和浊燃的正式静态策略已经固定为 50%：四者不吃面板暴击率，暴伤统一使用固定 50% 期望比值，
不受本击暴击识别结果影响。其他固定暴击率渠道使用各自已知概率，概率缺失保持未量化。噩梦、蚀心和鸩火在粗分中各自作为“持续直伤”展示，保留 DOT
标签并消费攻击、通伤及对应属性乘区；创生消费防御及自身正式属性抗性/穿透，浊燃消费防御和固定咒抗/咒穿，
黯星取消防御但保留心灵抗性/穿透。浊燃不消费攻击、通伤、咒属性伤害提升或角色暴击率，只消费固定 50%
暴击下的暴伤、环合强度及其正式目标/专属乘区。安魂曲五觉生命上限结算以实测有效伤害为锚，按正式来源
噩梦的候选/原始伤害加权比联动；新激活机制缺少实测下降时使用正式 200% 与实际生命比例，生命比例缺失才
使用 50% 期望。来源缺失时保持不可用，不能把生命结算忽略，也不能用残缺血量轴压低角色收益。
不再按最接近实际伤害重新选择暴击分支。逐击级方法、置信度和误差仍保留在 Qt 无关结果中供审计与后续适配器使用，主页面不铺开这些文本。

导入战报保存来源账号昵称、最近一次导出账号昵称及逐角色装备摘要。导入后外层与边际页的空幕/驱动均只读；
允许修改养成、觉醒、技能、弧盘和精炼，也允许“从角色页同步（不含空幕驱动）”。切换装备
来源、替换装备、读取游戏当前/已保存方案、含装备同步或创建新装备覆盖由 UI、Service 与 DAO 三层共同拒绝。
空幕主属性边际仍可在原始冻结装备上临时移除或替换主属性，保留其余装备、套装、副词条和来源；该计算请求
不开放装备编辑，不读取当前账号背包，也不写入修改副本或导出包。

弧盘缺失运行时事实时使用显式、可审计的模型默认：糖果骑士由第一轮逐击公式重放推断暴击后做一次有界
第二轮叠层重放；黑之书追加伤害暂按可暴击；思考喵按十层、茶花会暴击伤害按四层。错误的门只消费统一
治疗事件，不再把装备者 E/Q 动作直接猜成治疗。正常目标血线直接读取逐击的
`target_hp_before/target_max_hp`，不另设生命档位。这些均为用户确认的战报默认，不冒充官方常量。
其余缺失状态沿用已确认默认：玩家生命按高于 50%、护盾按不存在，未知 Boss/目标状态按不满足；能量、
冷却或理论召唤次数不会凭空补造动作或伤害。任何正式运行时事件都高于这些默认。

战报还会从本场冻结配装解析当前精炼弧盘、已满足件数的装备套装和明确选中的觉醒效果。套装件数由
已装备核心确定套装，再按驱动与该核心 `required_shape_ids` 的不同形状交集计数；同形状的重复驱动不能
重复补足套装件数，也不能按装备行的 `suit_id` 计数。二件套 `modify_pack` 作为整场常驻属性证据读取，四件套
由 24 条稳定空幕目录按已审计语义重放：Q/E 动作、普通攻击逐击、队伍分属性伤害、环合状态、持续时间和独立层数分别
进入专用规则；护盾和治疗效果不虚构固定伤害收益。专用空幕规则命中后跳过旧 generic links，最终生成
`battle-static-buff-v25` 推算区间。角色培养被动另由 44 条稳定目录按突破 2/4 解锁；确定的常驻、驻场、
技能限定与逐击叠层数值进入同一 Buff 区间，浔 Q 终结段进入技能倍率证据，法帝娅最大生命汲取按被动解锁
门控。资源、目标状态、召唤生命周期和新增逐击类被动保留专用适配器及固定轴估计策略，不冒充常驻面板。
真红的强化分支按正式 Rage 伤害项逐击确认，不从相邻命中补造完整升腾持续区间；强化增伤与觉醒倍率分别
消费正式静态曲线和伤害项绑定。威慑凝视及其追加伤害依赖本击凝视层数；完整轴可按最后主动攻击、弱证据
切人时点、扣停表空窗和觉醒规则估算，保留低置信度并说明起点、首层和取整假设。轴不完整或缺少起点时
保持未知，不默认一层或满层，也不以实测伤害反求层数。固定轴属性比较可保留已证明能相消的共同系数；改变
第三项或第四项觉醒会影响凝视增长、上限或新增命中，相关角色和团队收益必须携带机制缺口，不能显示完整零收益。
治疗来源先在 `battle-treatment-event-v2` 派生轴上按正式技能、动画时点与已重建状态区间生成事件，再由角色被动、觉醒和
弧盘共同消费。伊洛伊点按 E、QTE、Q 即时治疗以及 Q 的 14 秒、每秒一次持续治疗已结构化；Q 持续治疗
施加时不立即执行，周期与所有治疗触发 Buff 的持续时间均按扣除时停的活动时钟推进。当前版本还对齐战斗开始/结束、E/Q/QTE、通用技能开始/结束、逐击前后、
格挡/闪避反击和低置信换人事件；暴击、击杀、环合触发等没有正式证据的条件不强行命中。页面独立汇总
Buff 审计可见列固定为来源、作用对象、Buff、值、触发、时间覆盖、覆盖逐击、伤害覆盖率、收益率和详情；
同一 Buff 的多个属性在同一行按属性/值一一换行，不拆成多个收益行。值显示定值、百分比或“每层值 × 层数”，
未知 Calculation 明确显示尚未解析；触发使用集中维护的中文映射。时间覆盖显示区间并集时长/当前范围包含
时停的总时长；收益率只显示完整值、带“部分”标记的已知分量或破折号，不显示“已量化”字样。原始 GE、
区间、状态/数值置信度、推断依据和缺口收进详情。逐击日志的详情可查看哪些加成实际进入本击公式、哪些因
属性/作用对象/专用倍率不匹配而未采用、哪些仍缺数值或 Calculation 结构化。Buff 审计还会按稳定来源逐项执行移除反事实：在当前选定时段和真实逐击轴不变的
前提下删除该 Buff 的全部刷新/叠加区间，以移除前后公式期望比缩放原始逐击，并展示无此 Buff 的团队伤害、
伤害增量、时段收益率、覆盖逐击、团队伤害覆盖率与量化状态。收益率分母是无此 Buff 的时段伤害；各 Buff 独立移除，重叠与
乘区交互导致各行收益不可相加。尚未进入结构化公式的变化保留完整原轴并标为 `partial` 或 `unavailable`；
页面不得把原值占位、缺少画像或缺少 Calculation 显示成精确零收益。跨越范围边界的区间先与当前范围取交集，
再按并集合并覆盖时长；受影响逐击也只统计当前范围。伤害覆盖率的分母使用当前范围有效总伤害，分子除确认
覆盖的逐击外，还包括由这些逐击证据联动的生命上限结算。
移除某个 Buff 时只重放它实际覆盖的逐击，未覆盖逐击严格保持原值；这是长战报的计算懒加载边界，不改变任一组反事实结果。
冻结配装永久来源不进入数值边际，避免与冻结面板重复；未知 Calculation 仍只展示证据。当前 49 个弧盘全部具有独立
refinement 所有者：22 个既有专用模型、16 个伤害优先补全模型和 11 个生存/资源/运行时条件适配器。
专用链覆盖常驻、A/E/Q/QTE 消费者、Q/E 起止、前后台、受击、治疗、逐目标、显式暴击、周期叠层和召唤
派生语义；玩家生命、护盾、方斯、击杀、削韧、队伍属性或未命中证据门槛的主动调弧等事实缺失时只展示
参数与缺口，不进入伤害公式，也不补造逐击。预备备在 E 动作实际结束时取得左虎符、正式 Q begin 取得
右虎符，猫协同伤害碎片不计作 Q；十五个有效战斗秒内凑齐双符后，从第二枚令牌到达时建立十秒区间，不再
等待伤害阶跃。Boss 条件由正式解析目标的怪物目录分类消费，缺少分类时保持未解析且不显示零收益。行进于
时间之外的 Q 暴伤只覆盖该次 Q，满三层后的无视防御另建七十秒自身全伤害区间。精炼参数已确认一一对应的
阿剌克涅、妖刀、玫瑰和时间弧盘 Calculation 会保留原路径并按当前精炼参数解析数值。
极速之绵周期使用不扣时停的原始时间，离场清层；海特洛召唤只消费战报真实逐击，可暴击和继承持有者增伤
暂按低置信模型处理，不按存在期补造攻击。随机治疗/护盾、敌方输出降低、缺少 Boss 身份和缺少逐目标状态的
语义只保留审计证据，不冒充可安全投影的玩家伤害增益。
轨外之境上下半使用不同队伍时，角色来源 Buff 还会按正式逐击中的半场首次时间和出场角色截断：上半 Buff
不能延续到下半，下半角色的常驻推算从下半第一击才开始；魔女赐福等来源角色为 0 的外部环境 Buff 保持整场。
同一角色确实在两半都出现时拆成互不连续的两个区间，不把换半间隔算作覆盖时间。
当前与下一期轨外赛季 Buff 由静态 schema v26 随正式配置加载。`Abyss_8`「炽火灼痕」按正式浊燃逐击在
结算后增加全队 6% 暴击伤害，1 秒最多触发一次、整组持续 6 秒并刷新、最多 8 层；目标出现
`Buff_Tenacity_damage` 后，按该层/半场正式属性包的 `UnbalMax ÷ UnbalReduceReset` 在扣时停时钟上重建
逐目标倾陷区间，区间内同目标逐击获得 25% 通用增伤。`Abyss_9`「飘摇残响」把咒、灵属性伤害各提升
45%，只投影到属性匹配的逐击。配置 ID 仍表示整期环境，不表示层数。
妖刀由装备者归属的
咒属性直伤、DOT 与浊燃逐击正向重放：本击先使用命中前层数，结算后再叠层；0.3 秒触发冷却与 15 秒持续
时间都使用扣时停时钟，最多七层且有效重复触发刷新持续时间。空幕的森林萤火之心与真红：双生蝶按正式
分属性伤害事件逐层独立到期，静谧山庄按普通攻击的正式 Hit 叠层。其他实际叠层仍未知时不进入边际。动态区间
只将加法操作、已解析数值、作用对象明确且已映射乘区的属性投影为
`battle-buff-attribute-v22`：自身/全队/除来源外队员/明确 `character:<id>` 属性进入对应角色逐击面板，轨外倾陷增伤还会核对本次进入倾陷的正式目标 ID，低置信阶跃反推也只进入提供证据的同一目标；敌方七种 `DamageResist*` 减抗只进入匹配
伤害属性的敌方抗性区。同一来源的重叠区间按刷新去重；层数未知、非加法操作、未支持敌方乘区和未支持属性
仍保留排除原因。
倾陷逐击使用团队求和适配器：以出场队伍全体角色为固定格子，每格读取自己的官方等级基础值、固有伤害
属性、冻结倾陷强度、倾陷伤害提高、防御/属性穿透及该时刻适用于该角色的 Buff，完成防御和属性抗性结算后
再求和。逐击公式弹窗显示公共敌方倾陷档位、每名角色的完整代入和小计；触发事件归属角色不作为唯一伤害
来源。当前争锋赏宴 `UnbalMax=70` 的墨菲克斯使用真实逐击验证的 2500% 高阶 Boss 档。
残虹零觉由独立形态适配器从正式动作 GE 推算「狩 / 惑」区间：狩的 25% 进入自身通伤，惑的 25% 只进入
全队噩梦、蚀心、鸩火和浊燃等持续伤害暴伤；切形态技能优先使用动作证据最后一击作为正式附加时点，避免
动画展示窗与下一角色动作重叠时把刚进入的形态清除。形态持续与双向旧状态保留读取静态曲线并使用扣时停时钟。
一觉生效后不再使用零觉的切换区间，而是把狩 40% 与惑 50% 从战斗开始常驻至结束；切形态和离场均不
移除。当前协议没有玩家阵亡事件，无法提前截断。三觉生效时，惑持有区间每经过 1 个扣除时停的有效战斗秒
生成一次 `4% MaxHP` 自疗事件。
安魂曲噩梦最多 10 层；普通攻击、极限反击、原生 E 和 Q 的每个有效正式直伤 hit 都在该 hit 结算后施加
1 层，多段原生 E/Q 按实际 hit 数逐层增加，不把技能详情曲线值解释成一次施放直接增加多层；偷取后的
学习 E 不施加噩梦。默认每层持续 3 秒，只有
选中 `Effect4` 才延长至 6 秒；每层以扣时停时钟按自己的 hit 时点独立到期。导出的
`Cau_LacrimosaBloodDam` 只暴露 `StackCount/MaxCoef/StackMult` 等图节点名称，未提供蓝图字节码和运行时
默认值，所以逐层求和属于项目侧重放模型，不冒充原生 Calculation 函数体。
当有效噩梦施加 hit 与下一次噩梦跳伤相隔不超过 0.7 秒，但同目标最近 10 秒的实际单层伤害只支持少一层时，
详情以中置信显示“服务器少接收 1 层”的实伤反算结果。它只修正本击层数展示和边际缩放基准，不反写后续
状态；存在同伤害并发归属污染的逐击不参与该反算。蚀心、鸩火的 30 秒目标状态与噩梦各层都按扣时停的
有效战斗时钟到期，时停期间不推进。幻境形态普通/分支逐击各给蚀心增加 1 层，强化变轨增加 5 层；鸩火
只由「血宴入梦时」最终施加点增加 5 层。零觉幻境普攻本体的 DOT 扩散只刷新原目标已有鸩火的持续时间，
不增加鸩火层数；一觉额外开放的现实攻击扩散与多目标复制仍按各目标、原层数、剩余时间和周期隔离处理。
蚀心每跳公式把正式活跃层数与有效结算系数分开，只在“单份/正式满份”和“非暴击/暴击”组合中受约束匹配，
不开放任意 1～10 层逐跳拟合，也不把本跳选择反写后续状态。缺少施加事件但仍观察到 DOT 时只显示“至少一份”
低置信下限。
残虹突破被动启用时，普通浊燃正式记录由专属浊燃替换，不再维护一组可并行结算的普通层。专属浊燃按
`(半场, 目标)` 最多 3 层。战报固定轴先由浊燃反应存入 1 层未激活状态；随后每实际记录 1 个非浊燃 DOT
跳伤，就给该目标的残虹浊燃增加 1 层并激活整组伤害，满层后继续刷新 15 秒；中间未记录的跳伤不反推，
浊燃自身不递归触发。该逐击投影由本机历史战报的覆盖率和残差回归约束，只解释已经发生的固定轴；残虹浊燃
后续结算公式归属固定为残虹。
若某种已审计 DOT 的施加事件缺失，但本击前 1.5 秒内同半场同目标存在其正式跳伤，只在该短窗内把它计为
当前 DOT 种类的存在证据，供早雾被动消费；这不建立或刷新完整浊燃状态，也不反推任何层数。
`Buff_Zankou_ListenBattle` 的专用 Calculation 仍只绑定蚀心和鸩火，不扩展到浊燃。浊燃层数只由正向逐击
状态机给出，不用实际伤害拟合层数，避免把暴击、Buff 或目标参数偏差误判为层数。
边际以真实逐击为锚，在冻结与编辑后面板上同时加入该击的动态 Buff，只改变属性边际敏感度，不把 Buff 再乘到已观测伤害上。
`ScalableFloat.Value` 只是曲线系数；持续时间和数值必须先解析曲线行或当前精炼参数，曲线未解析
时不回退成错误的 1 秒或 100%。角色技能只消费 `character_combat_ability_binding` 中正式 GA 事件直接绑定、
且规范化定义确实含属性修正的 Buff/GE；主动技能按 A/E/Q/G/QTE 动作起点生成低置信区间，常驻
`passive_buff` 仅作为整场静态证据。仅存在于任意 Blueprint 引用闭包、但缺少施加事件的对象不据此伪造
激活。区间只在内存中重算，不写回正式逐击；未来 nte-core 实测按 Buff、对象、字段和时段逐项覆盖。

稳定 GE 优先使用精确中文语义；缺少专用语义时通过 `GE → GA` 回退到官方技能中文名。伤害项返回“未知伤害”
或“未识别伤害”时，展示层依次回退到已渲染来源技能和逐击携带的原始 Ability 名称，不让通用未知桶覆盖已有
技能身份；原始 GE 稳定标识继续只保留在战报事实和证据详情中。
逐击分类、攻击类型和伤害属性也只在展示层映射为中文。所有伤害导向区域统一按“有效伤害项 → 来源技能 →
未识别伤害”取名：时间轴伤害条、逐击点详情、技能明细和逐击日志都先展示伤害项，来源技能只作为次级说明；
动作条仍展示动作/技能名。例如残虹的 DOT 展示“蚀心/鸩火”，而不是父技能“普通攻击/极轨终结：燎原”。

统一轴的动作与输入投影只消费正式逐击：排除追加攻击、DOT、Buff、环合和覆纹等二次伤害后，按角色、稳定 Ability、
实际技能变体、输入类别和最大连续出伤间隔组合为动作段；普攻、E、Q、G 和 QTE 可显示从 GE 段号得到的
紧凑动作序列，闪避反击和格挡反击归入普通攻击动作类别；同一时刻的普通攻击与反击伤害只形成一次 A 操作。
推算输入只展示物理操作：普攻不同连段是相互独立的 A 点按；一次键盘按键产生的 E1/E2/E3/Q1 等技能段号仍合成一个 E/Q/G 输入。相同技能段再次出现时，仅在段号重置或静态 Montage 的命中 Notify 无法由一次释放覆盖时拆成下一次点按，不能只因 Ability 相同且时间接近就合并；若同一 Montage 的正式 Notify 能覆盖完整序列，即使内部 GE 数字不递增也保持为一次输入。普通 A/E/Q/G 点按投影为定宽方块，不沿用动作动画的完整时长。长按识别同时覆盖显式 `Hold/Skill2Hold/Skill3Hold`、`JumpSection_HoldTriggerCheck`、`Begin/Loop/End` 和 `Branch/持续输出分支`：蓄力 A 显示为 Z，蓄力 E/Q 仍显示 E/Q，不展示内部蓄力分支名。输入持续区间与动作动画区间独立；卡厄斯与早雾式释放后重击的伤害点位于长按条尾或其后，薄荷和伊洛伊式持续攻击的伤害点位于长按条内部。只有静态 Section/Notify 与正式 GE 序列能唯一支持时才采用对应关系，无法确认松手、未命中、无敌或中断时保留低置信边界。QTE 只作为切换角色的结果投影为无文字的角色小头像，
不展示虚构的 1/2/3/4 键。普通动作横条的基础窗口是首末出伤证据；Q 的第一击位于 nte-core 已知
type2/type3/type4 时停区间内，或在该区间尾
之后 350ms 内时，该区间只关联一个最匹配 Q，动作开始锚定到时停头，结束至少覆盖时停尾。Q 逐击位于
同一已知 Q 类型时停区间内时，静态动画不得否决逐击身份、扩展动作边界或被后续动画窗口截断；该动作只消费
记录时停与原始逐击。只有没有可用的 nte-core 记录时停时，正式逐击的
`Ability/GameplayEffect` 能唯一反向命中同一 GA 的事件绑定和 Montage Notify，动作条才以首击 Notify
对齐动画开始，并优先用 `BP_TriggerEndAbilityEffect`、结束技能事件或命中所在 Section 的结束点补足前摇、
后摇；辅助的消散、登场、预备和循环 Montage 不参与。多个候选得出不同边界、缺少正式 GE 事件或只能依赖
残虹同一次 Q 的 Magic/Force 两个固定形态段即使被中间逐击打断展示分组，也按同一正式 Ability、固定先后
顺序和不超过 8 秒的相邻边界合并；这只恢复一次 Q 动作，不把其他角色的内部 LTE AOE 当作 Q 输入。
异常资源总时长时，仍保留首末出伤窗口。同一 Montage 被多个输入选择器复用时，只读取该选择器命中的
Section 及其 `NextSectionName` 链，并将 Notify 时间归一到该入口，避免把 `Skill2Hold` 与 `LoopHold` 等
互斥段误认为同一次动作。后一个动作开始会截断同角色前一个动作的后摇。伤害条和逐击点始终
保持正式命中时间，不随动画窗口拉长。该边界用于在
“包含时停”口径展示完整终结技占时，在“扣除时停”口径由统一投影自然折叠；它仍不代表真实按键、重复点击
次数。v5 type6 区间不参与上述 Q 锚定；旧 v4 区间没有类型字段时沿用既有锚定兼容并标为类型未知，v5
压缩区间的 `pause_type_mask=null` 只参与有效时钟扣除，不伪装成 Q 或 type6。所有 nte-core 完整时停区间
的并集始终优先用于扣时；整场完全没有可用的记录区间时，统一时间投影才把已经推算出的完整 Q 动作
起止区间作为低置信时停回退，重叠区间合并后用于扣时停时钟、持续时间和时间轴展示，并明确标注来源，不写回
战报原始时停证据。有任一完整 nte-core 记录时停区间时不混入 Q 回退。该区间与 Q 的关联只用于动作展示和统一时钟，不能反向证明暂停由该 Q 触发。除上述静态长按程序外也不推测长按。动作身份置信度最高只到“中”；首末逐击窗口时间置信度为“低”，唯一静态动画事件对齐可
提升到“中”。页面同时展示进入推算的事件与伤害覆盖率，
并明确覆盖率不等于准确率；输入、动作、技能条或逐击点详情会列出其引用证据，但不会改动日志状态。结果带模型版本和逐击事件
引用，只在内存中重算，不写成战报事实。没有正式字段时仍不推测暴击结果、实测 Buff 状态、护盾、治疗
或环合激活；静态 Buff 区间始终明确标注为推算。实测输入、不可由当前静态事件唯一确定的动作分支和其他机制见
[路线图](roadmap.md)。

实时悬浮窗只在当前采集会话期间显示；点击“结束保存”或“放弃重录”立即隐藏，采集结束、错误和历史战报
也保持隐藏。重录的新采集以及下一次显式开始采集，仅在“显示实时悬浮窗”已开启的前提下重新显示。

历史战报列表优先展示用户确认环境；没有确认条件时读取账号库内版本化的自动目标推断快照并明确标注“推断”，
不得在打开列表时遍历旧战报同步重算。战报原始摘要、逐击轴与角色快照先完成保存，随后已有后台整场分析把
基础候选及残差裁决覆盖写入同一份派生快照；旧战报缺少快照时先显示原始深渊层数或“未知场景”，打开该场
并完成分析后再补齐。快照保存算法版本、静态 dataset、置信度和完整推断 payload；它不写入用户确认条件，
也不覆盖 Core 原始目标字段。手工记录与自动记录按账号保留策略清理，删除战报级联删除逐击、配装副本和
自动推断快照。结构化日志只记录记录 ID、
状态、导入/导出与跳过数量和安全错误，不记录账号昵称、导入导出绝对路径、原始摘要、逐击、装备 UID
列表或完整伤害表。

## 10. 工具与游戏资料库

工具页提供只读“游戏资料库”入口和“养成计算器”。资料库页面在每次刷新时冻结发行静态库的 dataset、schema、importer 和构建
时间；查询前重新核对发行身份，更新发生后要求刷新，不把新旧 dataset 混入同一页。首页展示角色、弧盘、
空幕与驱动、怪物与玩法、战斗机制五个游戏化档案入口，不提供“全部”、逐表覆盖总览、资源动画或来源追溯
入口。战斗机制图鉴只展示四类中文伤害公式，不把技能、GA、GE、Buff 或反事实覆盖拆成卡墙条目；schema 覆盖
审计、资源关系、反事实矩阵和来源追溯保留为内部能力，不作为玩家界面。

角色页速览展示至 80 级。技能按普通攻击、E、Q、QTE 和正式存在的 G 组成整行列表，允许编辑 1–10 级，
点击本行向下展开当前等级的正式倍率；其后展示每个逻辑角色在突破 2/4 解锁的两项正式培养被动，点击展开
中文说明，不伪造可编辑等级。养成只分等级养成和技能养成。觉醒玩家顺序为一觉至六觉、三觉、六觉，内部
仍保留正式普通效果与共鸣类型。弧盘页展示升级经验、面板、突破成本和混频技能。轨外之境展示全部编号
配置；没有正式开放时间的配置单列为未提供排期，但仍展示层、半场、怪物池、正式中文名和本次出场公式画像。
怪物卡片按正式出场成员消费其 `profile_set + pack_id`，不被缺少默认画像的通用模板覆盖。怪物身份不按中文名猜。
对象关系优先在当前详情直接渲染；缺少正式记录时省略入口，不生成失效跳转，也不把
unknown/unavailable 显示成 0 或倍率 1。详细边界见[游戏资料库](reference/static-catalog.md)。

角色与弧盘养成页只汇总升级、突破、技能和混频所需材料，不提供活力计算。已发布发行静态 schema v31 保存人物
逐级经验、突破阶段及成本、经验书规格与方斯消耗；角色页按“先最少经验溢出、再最少本数”汇总经验书，
并可选择是否计入沿途突破。技能页直接汇总正式逐级 cost。工具页的养成计算器使用图片卡片弹窗选择角色和弧盘，
角色页已保存的人物、弧盘等级/突破及技能等级会自动预填；它允许填写人物与弧盘的目标等级、突破前后状态和
各技能目标。角色选择器按显示名称去重并保留目录中的首个正式角色。技能按 A、E、Q、QTE 等正式输入分类显示，结果分别列出人物升级/突破/技能及弧盘
升级经验、突破消耗后合并成总材料清单。角色养成、弧盘养成和技能目标各有独立的图形开关，关闭后保留当前
输入供再次开启，但不进入本次计算；混频效果不影响本工具的养成材料数量，不作为输入或成本。当前发行静态库未提供弧盘经验材料规格时，仅显示正式升级经验，
不把它猜测换算为材料。计算器只读取账号数据，不写入角色档案或背包。所有入口均不直接读取外部 JSON，不把方斯或
任何材料折算为活力；缺少规范化记录时明确保留数据缺口。需要最低活力时只通过
[养成体力规划](reference/progression-stamina-calculator.md) 的确定产出 contract 计算。

## 11. 环境、更新与日志

环境诊断分别检查 Npcap、nte-core、dwmapi、备用 Mod Loader、Microsoft Visual C++ 运行库、运行时 SDK
缓存、IPC 管道和资源。插件默认在用户确认后备份并复制发行代理 DLL；代理方式不生效时可显式改用
管理员 Loader。两种加载方式互斥，Loader 会话由应用 stop event 和 owner PID 管理。工作区升级只更新
受管脚本并保留动态 SDK 缓存。游戏主程序自动检测先读取 Windows 卸载注册表，再检查各盘符下固定的
常见游戏库目录；检测不递归扫描磁盘，确认候选后才把路径保存到当前账号偏好。启动 Loader 时从该
`HTGame.exe` 的官方目录结构定位同一安装根中的启动器，并显式交给 Loader；监控进程存活不等于插件
已加载，装备 IPC 管道存在才是可用确认。Loader 不设固定哈希门禁，用户可直接用可信来源的同名文件
覆盖随包 `nte-mod-loader.exe`；替换文件仍会以管理员权限运行。

启动 Loader 前检查游戏目录的 `dwmapi.dll`：与当前发行插件或账号部署记录哈希一致时直接移除；旧版
或来源未知时，先复制到当前账号的插件备份目录并复核源文件与备份 SHA-256，再移除游戏目录文件。备份、
校验、删除或 Loader 启动任一步失败时中止操作；如果文件已经移除但 Loader 启动失败，则恢复原 DLL。

Mirror Controller/Integration 负责版本检查、下载、取消和安装器启动。日志不记录 CDK、Token 或鉴权 URL。

常驻日志为账号目录下 `nte_runtime.log`；用户开启详细日志时创建独立时间戳文件。账号切换结束旧账号会话
并在新账号目录创建会话。稳定事件名、公共字段和脱敏边界见 [日志事件规范](reference/logging-events.md)。
